合理拆分文件和目录，避免单文件过大，也避免过度拆分。
按功能和职责组织代码，保持结构清晰。
前端优先复用已有组件，避免重复实现。
基础 UI 优先使用 shadcn/ui。
保持实现简单、易维护，避免不必要的抽象。
数据与结构分离，方便i18n国际化。
路由、业务逻辑、数据访问合理分层，避免全部写在一个文件。
做好报错记录，日志记录。
原型在docs/ui-mockups中。
每次写完代码自动commit。
