# AGENTS.md

* 合理拆分文件和目录，避免单文件过大，也避免过度拆分。
* 按功能和职责组织代码，保持结构清晰。
* 前端优先复用已有组件，避免重复实现。
* 基础 UI 优先使用 `shadcn/ui` 和 Tailwind CSS。
* 保持实现简单、易维护，避免不必要的抽象。
* 数据与结构分离，方便 i18n 国际化。
* 路由、业务逻辑、数据访问合理分层，避免全部写在一个文件。
* 接口、类型和数据模型保持统一。
* 做好异常处理、报错记录和日志记录，不静默吞掉错误。
* 原型位于 `docs/ui-mockups`，实现页面时以原型为参考。
* 完成开发任务后自动commit。
