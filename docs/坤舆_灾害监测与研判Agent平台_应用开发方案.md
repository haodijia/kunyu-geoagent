# 第一届东方航天港杯开放地球引擎（OGE）应用创新大赛
# 遥感应用案例开发方案（赛道一）

| 项目 | 内容 |
| --- | --- |
| 作品名称 | 坤舆智枢——基于 OGE 的场景化灾害遥感研判智能体平台 |
| 报名单位 | 【待填写：单位全称】 |
| 团队成员 | 【待填写：全体成员姓名】 |
| 指导老师 | 【待填写：姓名】 |
| 队长及电话 | 【待填写：姓名、联系电话】 |

## 一、作品概述

**产品定位：“坤舆智枢”面向县域应急研判与灾后核查，以 OGE 的多源时空数据、分析算子和在线算力为核心，将“数据接入—信息提取—影响研判—核查反馈—成果更新”组织为同一条业务链。平台的自研 GeoAgent Harness 不直接替代遥感算法或专业判断，而是通过可执行的 GeoSkill 场景包约束计划，调用 OGE 能力，并把每项结果绑定到观测日期、有效范围、方法版本与任务成果。2025 年贵州榕江洪涝作为完整示范，灾后植被状态跟踪作为轻量扩展场景，用于验证新能力可在不修改 Agent 运行内核的条件下接入。**

### 1.1 作品背景与选题动机

我国自然灾害类型多样，不同地区面临的监测对象、发生时段与影响方式存在差异。国家防灾减灾救灾委员会办公室、应急管理部发布的 2025 年全国自然灾害情况显示，洪涝和地质灾害、地震、台风、风雹等灾害造成不同程度影响，干旱及森林草原火灾等也有发生。灾害管理需要持续掌握区域环境变化、承灾对象分布及灾后恢复情况。[应急管理部：2025 年全国自然灾害情况](https://www.mem.gov.cn/xw/yjglbgzdt/202601/t20260116_592135.shtml)

洪涝研判关注水面扩张与居民点、道路和耕地的空间关系；农业干旱分析关注降雨、植被和地表温度的持续异常；山火灾后分析关注火烧迹地范围与植被恢复；地质灾害迹地核查关注坡面变化及周边设施。业务方法随灾种变化，工作过程则共同涉及观测资料获取、空间信息提取、影响对象识别、重点区域核查和后续跟踪。县域业务人员需要将这些信息落实到具体乡镇、地块和设施，形成可执行的核查安排与阶段研判成果。

遥感能够提供大范围、多时相的地表观测，光学、雷达与热红外数据可结合地形、气象和土地覆盖资料开展专题分析。实际工作中，数据检索、质量筛选、预处理、算法配置、空间统计与报告编制往往分散在多个工具中。同一区域更换事件时段或分析主题后，数据准备和流程配置需要反复进行；图层、统计和核查记录分别保存，也使成果更新与历史比较增加了额外工作。

OGE 集成多源时空数据、分析算子与在线计算能力，可承载影像处理和专题计算。业务应用需要进一步组织数据选择、算法衔接、任务执行和成果交付，将计算结果转化为区域变化、对象影响和核查依据。专业方法还需要保留适用条件、质量规则与案例经验，使不同事件的分析能够沿用已有积累。

“坤舆智枢”采用地图与智能交互协同的工作方式：用户确定区域、事件和分析目标，Agent 加载对应 GeoSkill 与项目 Memory，通过授权 Tool 调用 OGE 数据和算法服务；结果同步呈现为专题图层、时序曲线、区域统计和核查清单。Agent 负责需求结构化、场景匹配、候选计划和有引用的结果解释；数据校验、类型检查、任务执行和写操作由确定性服务与人工确认门禁控制。平台将项目资料、分析过程、现场反馈与成果版本统一保存，为连续研判和场景复用提供依据。

### 1.2 作品定位与建设目的

平台面向县乡应急、水利、农业农村、林业及自然资源业务人员和遥感技术服务人员，围绕“哪些地方发生变化、涉及哪些对象、优先核查哪里、后续如何跟踪”组织功能。技术人员维护数据、算法服务与场景方法，业务人员在地图工作台中提出需求、查看成果并安排核查。首个洪涝场景服务县域及乡镇的灾中核查与灾后复盘，输出疑似新增积水、疑似受影响耕地及重点核查对象。项目、场景、任务和成果之间保持明确关联，便于连续工作和方法复用。

作品拟实现以下目标：

1. **提高研判效率。** 将数据准备、灾害专题信息提取、承灾对象叠加、区域统计和报告生成组织为连续任务，减少环节之间的重复操作。
2. **沉淀专业方法。** 将数据选择、算法组合、参数确定、质量检查和结果解释组织为版本化 Skill，形成可以在不同项目中调用和改进的场景方法。
3. **服务现场核查。** 形成包含位置、面积、关联对象和核查要点的工作清单，支持从乡镇概况逐级查看具体图斑。
4. **支持复查与扩展。** 关联影像、算法、Skill、任务及成果版本，保留项目记忆和复核经验；通过增加算法服务与场景包，扩展灾后植被恢复、干旱影响及其他灾害专题分析。

系统按照业务职责协同运行：React 前端以地图工作台呈现数据、对话、计划、统计和核查；Python/FastAPI 后端承担业务服务、任务调度、存储、权限与 OGE 接入；OGE 脚本及算子完成专题计算。自研 GeoAgent Harness 组装模型、工具、场景方法、记忆与策略能力，但不绕过后端的参数校验、权限与任务状态机。地图、统计表和报告共享同一结构化成果与版本记录。

### 1.3 示范区域与案例设置

示范案例采用 **2025 年 6 月贵州洪涝，以黔东南州榕江县为主要分析区域**，围绕都柳江沿岸及县城周边的居民点、道路和耕地开展水面变化与影响分析。

2025 年 6 月，持续强降雨与上游来水叠加，都柳江榕江、从江段遭遇特大洪水。6 月 24 日，国家防总针对贵州启动防汛四级应急响应，派出工作组赴榕江、从江等重点地区指导抗洪抢险；6 月 28 日，榕江再次遭遇洪峰。两轮洪水相继发生，增加了灾情持续跟踪、重点对象核查和灾后恢复工作的需求。[贵州省水利厅](https://mwr.guizhou.gov.cn/gzdt/slyw/202506/t20250627_88196074.html)，[应急管理部](https://www.mem.gov.cn/xw/yjglbgzdt/202506/t20250624_547360.shtml)，[贵州省国资委](https://gzw.guizhou.gov.cn/xwzx/qyzc/202506/t20250630_88212673.html)

6 月 24 日贵州省公路部门发布的路况信息记录了榕江县部分国道路段因洪水漫路造成交通阻断；水利部门同期开展供水设施抢修。受影响对象分布与恢复进展需要结合遥感观测、道路及设施资料和现场记录持续更新。案例据此组织水面变化、居民点与道路关联、耕地叠加及重点核查清单，保存各项判断对应的观测日期和业务资料。[贵州省交通运输厅](https://jt.guizhou.gov.cn/cxfw_0/lkxx_5948603/gxgllk_5948605/zdxx_5948607/202506/t20250624_88180838.html)，[贵州省水利厅](https://mwr.guizhou.gov.cn/gzdt/slyw/202506/t20250627_88196074.html)

影像检索按灾前、两轮洪水影响和灾后恢复四个阶段组织，初设时间窗口如下。

| 阶段 | 拟检索窗口 | 分析内容 |
| --- | --- | --- |
| 灾前参考 | 2025 年 5 月 20 日—6 月 17 日 | 结合前期降雨筛选基准影像，确定原有水体和承灾对象分布 |
| 第一轮洪水影响 | 2025 年 6 月 18 日—6 月 27 日 | 对照 6 月 24 日洪水过程，提取各有效观测时相的水面变化 |
| 第二轮洪水及退水 | 2025 年 6 月 28 日—7 月 5 日 | 分别与灾前及上一有效时相比较，记录再次扩张、持续积水和退水区域 |
| 灾后状态跟踪 | 2025 年 7 月 6 日—8 月 31 日 | 跟踪受关注区域后续状态，结合地类、物候与对照区域筛查植被持续异常 |

每个窗口按卫星实际过境时间、空间覆盖和质量条件选择影像；事件时间线标注 6 月 24 日与 6 月 28 日两次洪水过程，各观测帧保存采集日期和有效范围。灾前参考避开前期已受洪水影响的观测，状态跟踪选用物候和地类条件可比的参考资料。

研究区以榕江县行政边界组织数据和总体统计，以乡镇及具体图斑组织核查。先在具有有效观测的沿河开放水面与耕地区域完成分类及参数校准，再扩展县域分析；城区结合道路、居民点和现场记录开展对象级复核。地形辅助层用于识别山地阴影及解释河谷与承灾对象的空间关系。后续观测接入后，更新同一事件的图层、清单和报告版本。

### 1.4 建设内容与预期成果

交付产品由三部分组成：一套 React + Python/FastAPI 灾害监测与研判 Web 平台，一套自主设计的 Agent 运行与能力管理模块，以及一套基于 OGE 的洪涝场景服务包。平台以地图工作台组织数据、分析、核查和成果操作；Agent 模块包含 Tool、Skill、Memory、运行循环和执行记录；洪涝场景包包含 OGE 脚本与服务绑定、分析流程、参数规则、图层样式、统计字段和报告模板。三部分集成部署后，用户可以完成从项目创建到成果归档的一次完整分析。

本期以开放水面变化识别及其与耕地、居民点、道路的空间关联为主要分析内容。专题成果用于提供灾情核查线索，核查人员可结合现场记录进一步确认影响情况。最终形成影像与辅助数据目录、水体变化图层、耕地影响统计、重点对象清单、研判报告及部署操作说明，为后续推广到其他县域提供可复用的案例基础。

### 1.5 最终产品形态与典型使用过程

系统以 Web 应用交付。用户选择区域项目后进入地图工作台，地图占据主要空间，右侧 Agent 面板承接需求与连续分析，左侧显示数据和图层，底部按需展开时间轴、统计与核查清单。每个项目保存区域底图、灾害事件、分析任务、成果版本和核查记录；顶部切换事件与场景，主要分析过程在当前页面完成。

以“榕江县2025年六月洪涝研判”项目为例，用户的操作过程如下。

| 用户操作 | 页面上出现的内容 | 系统形成的业务对象 |
| --- | --- | --- |
| 创建项目并选择榕江县 | 项目名称、区域边界、时间范围 | 项目与研究区 |
| 在顶部选择“洪涝影响分析”，或向 Agent 描述目标 | 场景用途、所需数据、参数表单和预期成果 | 当前场景及 Skill 版本 |
| 选择影像，或向 Agent 描述分析目标 | 候选影像卡片、覆盖范围和步骤计划 | 数据选择与任务草案 |
| 提交分析 | 地图旁显示执行步骤和处理状态 | 正式任务、步骤依赖与 OGE 作业 |
| 查看完成结果 | 新增积水图层、乡镇统计和重点对象清单 | 图层、统计表及空间对象 |
| 点击某个乡镇并提出“查看这里的耕地影响” | 地图定位、区域统计及相关图斑 | 基于现有成果的查询或进一步分析任务 |
| 复核图斑、导出报告 | 核查意见、专题图和报告预览 | 复核记录与成果版本 |
| 在下一次降雨后继续分析 | 已保存的区域、数据目录和适用场景 | 复用项目上下文的新任务 |

项目持续保存专题地图、空间数据、历史任务、核查记录和报告。用户再次进入项目时，可加载已有成果进行时相比较、补充核查或发起后续分析。

对技术人员，系统提供独立的“场景与方法管理”入口，用于登记 OGE 算法服务、维护 Skill、配置表单和成果模板、发布新的场景版本。业务人员在场景库中选择已发布的方法；技术人员通过方法管理将专业经验转化为后续项目可使用的能力。

### 1.6 核心创新与可验证性

本作品的创新定位是**面向 OGE 遥感业务的应用模式与系统集成创新**，不将 NDWI/MNDWI、SAR 阈值或通用大模型工具调用表述为原创算法。核心差异及其验证方式如下。

| 核心创新 | 现有痛点 | 本作品的实现机制 | 可验证证据 |
| --- | --- | --- | --- |
| GeoSkill 可执行场景契约 | 算法、表单、质量规则、图层样式和报告模板分散，换场景需重复开发 | 一个场景包统一声明 `manifest`、`SKILL.md`、`workflow`、`validation`、`presentation` 和 OGE 能力绑定 | 在不修改 Agent 运行内核的条件下发布、运行和停用第二场景；记录宿主代码改动量与接入时间 |
| 受约束的双层研判 | 纯对话 Agent 容易绕过业务规则，固定表单又难以处理歧义与条件分支 | LLM 作为控制面，负责意图、场景、候选计划与解释；确定性工作流作为执行面，负责类型、权限、预算、幂等与质量门禁 | 对正常、歧义、缺数据、权限失败和结果冲突任务测试合法计划率、工具参数正确率、任务成功率和受控拒绝率 |
| 空间语境与成果证据链 | 对话中的“这里”“上一期”与地图对象容易脱节，图、表、文更新后可能口径不一 | 将地图选区、观测帧、图斑、OGE `processId`、算法/Skill 版本和成果字段组织为稳定引用，图、表、文共用一个成果版本 | 从 Agent 结论反查输入影像、有效范围、参数、作业与成果；统计引用完整率和图表文版本一致率目标为 100% |
| 从遥感像元到核查行动的闭环 | 专题图仍需人工拆分为乡镇、耕地和设施核查任务 | 在共同有效观测内生成疑似新增水体和疑似暴露对象，形成可定位、可排序、可回填的核查清单 | 对比人工多工具流程、固定表单流程和 Agent 场景流程的操作步数、主动操作时间、错误率与成果一致性 |

与普通“WebGIS + 聊天框”的核心区别是：**大模型不直接生成灾情事实，而是在可版本化的场景契约下组织 OGE 计算；结论必须引用结构化成果，缺少证据时降级为“观测不足”或“待核查”。**

## 二、需求分析

### 2.1 应用领域与主要用户

作品面向县域灾害观测、影响研判、现场核查与灾后状态跟踪，首个应用落在防汛应急和农业灾情核查。主要使用人员及任务如下。

| 用户 | 业务任务 | 所需信息 | 系统提供的支持 |
| --- | --- | --- | --- |
| 县乡应急工作人员 | 初步了解影响分布、安排巡查 | 重点片区、观测时间、待核查对象 | 区域地图、核查清单、任务摘要 |
| 水利业务人员 | 对照河道周边水面变化 | 灾前灾后范围、河谷变化、异常图斑 | 双时相对比、常年水体参照、地形辅助层 |
| 农业农村业务人员 | 安排耕地受影响核查 | 疑似受影响耕地位置及乡镇分布 | 耕地叠加、面积统计、图斑导出 |
| 林业与自然资源业务人员 | 开展灾后状态跟踪及后续灾害专题核查 | 植被时序、地表变化与设施关联 | 持续异常图层、变化曲线；按场景接入火烧迹地和地质灾害迹地分析 |
| 遥感技术人员 | 调整方法、复核结果、形成报告 | 数据质量、算法参数、任务记录 | 专业参数面板、执行记录、版本对照 |

### 2.2 现有工作中的主要困难

**观测时效与数据质量需要统筹。** 洪涝与山火需要及时获取事件前后观测，干旱与灾后恢复需要连续且可比的时间序列。影像获取时间、云雨遮挡和空间覆盖共同影响研判效果。系统需要在候选数据中综合比较这些条件，选取与事件进程相匹配的影像，并向用户展示覆盖范围和实际观测日期。

**处理流程分散，容易重复劳动。** 同一类灾害专题分析与空间叠加流程需要在不同地区、不同时间重复配置。作品将稳定的专业步骤编入 OGE 脚本，将区域、影像引用和阈值作为参数，使新增任务能够复用经过验证的处理流程。

**空间成果需要转化为业务信息。** 业务人员关注的是乡镇间的影响差异、耕地重叠范围和重点对象分布。系统需在水体图层基础上完成行政区、耕地及设施叠加，将连续的空间分布组织成可定位、可排序、可导出的核查清单。

**报告与数据容易脱节。** 手动填写面积和排名时，图层更新后文字可能没有同步修改。系统以同一个成果版本生成地图、表格和报告，并保留字段引用，避免不同环节使用不同口径。

**复杂地物条件需要精细判读。** 云影、雷达阴影、水田灌溉和影像配准误差都会影响水体识别。系统应结合质量掩膜、地形信息和人工复核，提高结果可靠性，并在地图中清晰呈现有效观测范围与需要进一步核查的图斑。

### 2.3 功能与质量需求

| 需求类别 | 具体要求 | 对应验收依据 |
| --- | --- | --- |
| 任务创建 | 支持自然语言、行政区选择和地图绘制范围；明确时间窗口及关注对象 | 任务配置能够保存、回看并重新运行 |
| 数据准备 | 检索 OGE 产品、影像和个人资产，显示时间、波段及质量信息 | 每个入选影像都有数据来源与选择理由 |
| 遥感分析 | 在 OGE 开发中心完成预处理、水体变化和叠加统计 | 提供可独立运行的脚本及对应成果 |
| 智能编排 | 按工具返回结果推进任务，遇到数据或权限缺失时停止相关步骤 | 执行记录包含工具输入摘要、返回状态与任务编号 |
| 成果展示 | 地图、乡镇统计、图斑详情及核查清单联动 | 点击统计项可定位对应空间对象 |
| 结果解释 | 区分有效观测、疑似积水、参照水体和待核查区域 | 图例、表格与报告使用一致分类 |
| 过程追踪 | 保存数据、参数、任务依赖、结果与人工复核记录 | 指定结论能够反查到原始结果 |
| 场景管理 | 登记算法、发布 Skill，配置输入表单和成果模板 | 场景版本可选择，并与任务关联 |
| 连续工作 | 保存项目设置、会话状态和案例经验 | 再次进入项目可以继续分析，记忆条目可管理 |
| 扩展复用 | 通过场景包接入新的防灾减灾方法 | 完成灾后植被状态跟踪场景的轻量接入演示 |
| 时序与地形交互 | 实际观测时间轴与动态图表联动；重点片区三维地形列为交互加分项 | 切换时相同步更新地图和统计；完成三维时再检查二维与三维定位一致性 |
| 服务复用 | 将完整案例封装成标准后台服务 | 外部客户端可提交分析并获取相同版本成果 |
| 运行保障 | 凭证由后端管理，任务异步执行，提交请求统一去重 | 完成鉴权、断连重连和重复请求的人工联调检查 |

### 2.4 实用性与本期范围

作品的实用价值在于缩短“找到合适数据”到“形成可核查清单”的工作过程。应急人员可据此确定关注片区，农业人员可定位需要走访的耕地，遥感人员可集中开展疑点判读与方法校准。核查意见回填后，平台更新对象状态和报告，供后续跟踪与复盘使用。

本期交付按“核心链路 + 鼓励项 + 扩展验证”分层组织。核心链路完成 OGE 光学洪涝脚本、承灾对象叠加、核查清单、受约束 Agent、二维地图、动态图表、观测时间轴、可追溯报告和 RESTful WebService；SAR 多源交叉核查在输入与预处理条件确认后实现。灾后植被状态跟踪只做小范围第二场景，用于验证 GeoSkill 场景契约的扩展性；三维地形为时间允许时完成的交互加分项，不影响主链验收。干旱、山火和地质灾害属于后续扩展设计，不列为本期已实现成果。

### 2.5 从观测结果到业务行动

| 业务环节 | 平台处理与成果 | 支持的工作决定 | 反馈与后续使用 |
| --- | --- | --- | --- |
| 灾前资料准备 | 保存行政区、耕地、居民点、道路及基准影像 | 明确监测范围和重点保护对象 | 在区域项目中维护资产与关注清单 |
| 事件观测 | 对照降雨过程，提取多时相水面变化与有效范围 | 确定需要重点查看和补充观测的片区 | 后续影像到达后继续同一事件任务 |
| 影响研判 | 叠加承灾对象，形成乡镇统计和重点图斑 | 确定核查片区、对象和顺序 | 保存排序规则与调整记录 |
| 核查组织 | 导出位置、关联对象、依据和待确认事项 | 分派核查对象，记录负责人和处理状态 | 回填现场情况、照片及实际影响 |
| 成果更新 | 将遥感判读与现场记录关联到同一图斑 | 更新研判简报，确定仍需跟进的对象 | 保存核查前后版本，形成案例经验 |
| 灾后跟踪 | 比较后续有效观测与灾前参考 | 选择持续观察的地块和后续核查范围 | 沉淀状态跟踪 Skill 与历史时间序列 |

核查流程按“待安排—已安排—已反馈—已复核”保存状态。现场记录包括时间、位置、现象描述、附件和复核意见，与遥感观测日期并列展示。新增影像或核查反馈触发成果更新，形成“数据接入—信息提取—影响研判—核查反馈—成果更新”的业务闭环。

## 三、功能设计概述

### 3.1 系统总体架构

系统采用“项目工作台—智能控制面—确定性执行面—OGE 计算”的分层结构。React 前端承载项目、地图、场景、任务和成果；GeoAgent Harness 作为控制面，通过 Tool、Skill 和 Memory 形成可编辑候选计划；Python 业务服务、规则校验和任务状态机作为执行面，对计划做类型、权限、参数、幂等和质量校验后调用 OGE。场景包绑定 OGE 算法、流程与成果模板，模型无权越过该边界直接执行空间计算或写入业务数据。

![图1 系统总体架构](方案配图/图1_系统总体架构.png)

**图 1 说明：** 用户从项目工作台选择场景，通过表单或智能面板提交分析。业务层管理项目、数据、任务、成果和核查记录；Agent 与业务层共用服务接口。场景能力层保存版本化 Skill、算法绑定及成果模板，OGE 适配层负责计算提交与结果读取。持久化存储同时保存业务记录、项目记忆和执行轨迹。

项目统一保存研究区域、事件、任务和成果，场景配置规定分析方法与输入输出。参数表单根据场景定义生成，Agent 依据对应 Skill 编排任务，两种操作共用算法绑定、参数校验与成果结构。新增场景接入后，结果由现有地图、统计和任务组件加载。

#### 3.1.1 页面布局与操作入口

![图3 产品工作台布局](方案配图/图3_产品工作台布局.png)

**图 3 说明：** 主界面围绕地图组织操作。顶部确定项目、事件和场景，左侧管理数据图层，右侧 Agent 面板呈现需求、计划和分析反馈，底部展示时间序列、统计及核查对象。任务、成果和历史记录通过抽屉展开，用户保持当前地图位置继续工作。

| 界面区域 | 内容与作用 | 与其他区域的联动 |
| --- | --- | --- |
| 顶部项目栏 | 区域项目、灾害事件、场景、历史成果及导出入口 | 切换事件后同步加载相应数据与会话 |
| 左侧数据与图层栏 | 候选影像、辅助资料、变化图层、质量层 | 预览覆盖，控制显隐、顺序与透明度 |
| 中央地图 | 圈定范围、卷帘对比、对象选择、二维与三维查看 | 地图选区进入 Agent 上下文，图斑关联统计与核查 |
| 右侧 Agent 面板 | 需求输入、数据卡片、可编辑计划、步骤摘要和追问 | 工具结果直接生成图层、统计与成果入口 |
| 底部分析面板 | 观测时间轴、动态图表、核查清单 | 选时相切地图，点统计定位区域，点清单打开对象 |
| 任务与成果抽屉 | 历史任务、执行详情、成果版本及文件 | 加载历史结果后继续查询、比较或复算 |
| 场景与方法管理入口 | 算法登记、Skill 编辑、模板配置、版本发布 | 发布后更新业务场景选择器与 Agent 方法目录 |

首次进入项目，Agent 展示可直接使用的需求示例；用户也可选择场景启动分析。系统将区域、日期、输入数据和分析步骤整理为计划卡片，用户可直接调整。执行期间保持地图可操作；成果就绪后自动加入图层列表，并在 Agent 面板中显示摘要与下一步操作。

#### 3.1.2 业务对象之间的关系

一个区域项目可以包含多个灾害事件，每个事件记录类型、时间窗口、相关资料和关注对象；同一事件可以使用多个分析场景。每次运行形成一个任务，固定输入、算法与 Skill 版本，并产生图层、统计和报告。Agent 会话关联项目、事件和当前任务，用户选择的区域、图层或图斑随请求传入，使“这里”“这次结果”“上一期影像”等表达具有明确指向。任务记录、场景方法和项目记忆共同支持连续分析。

### 3.2 洪涝影响分析业务流程

![图2 洪涝研判业务流程](方案配图/图2_洪涝研判业务流程.png)

一次完整任务按照以下步骤运行。

1. **确定研判问题。** 用户通过参数表单或自然语言填写地区、事件时间和关注对象，系统形成结构化任务。对同名地名、缺少时间或边界不明确的情况，先在界面中提示补全。
2. **检索和检查数据。** 读取平台产品及影像目录，获取行政区与辅助图层，核对覆盖范围、实际过境时间、波段和质量信息，形成候选清单。
3. **确定分析路径。** 具备有效光学影像对时执行光学流程；具备同轨、同极化且预处理合格的 SAR 影像对时执行 SAR 流程。两类数据均合格时分别计算并交叉核查。条件不满足的区域标为观测不足。
4. **提交 OGE 分析。** 运行数据对齐、水体提取和变化分析脚本。后端保存 OGE 返回的 `processId`，按任务依赖等待结果，前序成果通过校验后传递给下游步骤。
5. **形成影响信息。** 将疑似积水与耕地、乡镇、居民点和道路叠加，计算有效观测范围内的面积、比例及空间关联。
6. **组织重点核查清单。** 根据疑似积水规模、耕地重叠和重点对象暴露情况排序，同时单列证据不足但业务上重要的区域。
7. **展示与人工复核。** 用户查看双时相影像、图斑和参数，对疑似灌溉水面或阴影等作出复核标记。复核意见保存为新版本，与原始计算结果关联。
8. **生成并导出成果。** 从当前成果版本生成地图、统计表、核查清单与 Markdown 研判报告，保留数据来源和有效观测范围说明。

### 3.3 任务输入与数据选择模块

任务入口提供自然语言输入、行政区选择及地图绘制三种方式。自然语言用于表达业务目的，地图与参数卡片用于核对最终范围。例如，用户可输入：“分析榕江县 2025 年 6 月两轮洪水期间的疑似新增积水，统计乡镇耕地影响，列出重点核查片区。”系统随后显示区域边界、灾前及事件期检索窗口、输出内容和当前数据条件。

数据清单包括数据名称、来源、产品级别、采集时间、坐标系、像元间距、波段、有效观测比例和入选原因。系统先以整景云量筛选候选影像，再利用质量层评价研究区内的有效像元，结合栅格边界确认实际覆盖范围，形成可供人工复核的数据选择记录。

### 3.4 遥感专题分析与质量检查模块

分析模块提供灾前水体、事件期水体、疑似新增积水、退水变化和无效观测区图层。除主要分类结果外，还输出所用阈值、共同有效范围和质量标记，使用户能够理解某一片区为何被识别或为何无法判断。

针对贵州山地与河谷相间的地形条件，系统综合使用产品质量层、DEM 辅助信息和人工复核标记，识别云影、山影及雷达阴影等干扰。耕地区域结合灾前状态、影像纹理和季节性用水特征开展判读，提高对洪涝积水与正常灌溉水面的区分能力。

### 3.5 耕地影响与重点对象分析模块

系统按乡镇汇总疑似新增积水面积、有效观测面积、疑似受影响耕地面积及其在有效观测耕地中的占比。对居民点和道路，提取与疑似积水相交或接近的对象，并附上判定方式、数据日期和待核查事项。

核查清单以图斑或业务对象为基本单元，包含位置、所属乡镇、面积或长度、关联对象、排序依据和证据状态。点击条目后地图定位到对应范围，并显示灾前灾后影像，方便业务人员安排后续核查。

### 3.6 任务监控与智能辅助模块

任务监控面板按“数据检索—质量检查—水体提取—变化分析—空间统计—报告生成”展示步骤状态。用户可查看参数摘要、OGE 任务编号、执行时间和结果链接，并通过任务中心管理历史任务、重新运行和版本比较。智能辅助面板提供数据选择说明、异常原因解释和后续分析建议，与任务监控共用执行记录。

任务进度以 OGE 返回的未开始、运行中、成功和失败状态为依据，配合已耗时及步骤完成情况展示。鉴权、服务发布、数据和参数问题分别归类提示，并关联处理入口。步骤异常时暂停其下游依赖，保留已完成成果与执行记录，支持处理后继续编排。

### 3.7 地图、时序与报告模块

**地图承接空间操作。** 平台支持灾前灾后卷帘、图层开关、透明度调整、乡镇定位和图斑查询。基准水体用蓝色，疑似新增积水用橙红色，核查对象以描边突出，无效观测区用灰色斜线表示。点击图斑可查看输入影像、面积、关联对象及核查记录，并向 Agent 继续提出“比较这里前后两期”“查看附近居民点”等请求。

**动态图表承接区域比较。** 乡镇条形图同时展示疑似受影响耕地面积与有效覆盖信息，时间折线图展示可比观测下的水面变化，统计项与地图共用区域和成果标识。用户切换乡镇、日期或图层后更新相应图表，点击排名项定位地图。面积变化同时保留统计范围和覆盖率，便于判断变化来源。

**时间轴承接事件演变。** 将降雨过程、卫星观测、分析成果与现场核查记录放在同一时间轴上。播放按实际有效观测逐帧切换图层，每帧注明采集日期、传感器、覆盖和成果版本；不同日期的水体变化在共同有效区域内比较，原始有效覆盖单独可查。缺测时段在时间轴留出间隔。用户据此观察积水扩展、持续及消退，并定位需要补充资料的阶段。

**三维视图承接地形判读。** 选取一个示范重点片区，利用 DEM 地形和卫星影像提取的积水范围构建三维查看模式，叠加居民点与道路，支持旋转、倾斜、定位及高程查询。其用途是解释河谷、坡地与核查对象的相对空间关系。地形展示保存 DEM 来源、分辨率和高程基准，积水图斑沿地形贴地显示，成果属性采用遥感观测范围与时间。

**核查与报告承接工作成果。** 核查卡片记录负责人、处理状态、现场描述和附件，复核后的信息进入当前报告。报告按“任务与数据—观测结果—乡镇统计—重点核查区—后续工作”组织，由 Python 报告服务根据成果模板与结构化统计生成，Agent 提供有成果引用的解释摘要。地图包含标题、图例、比例尺、方向、坐标系、数据来源和实际观测日期；报告导出 Markdown，专题图导出 PNG，空间数据使用 GeoTIFF、GeoJSON，表格使用 CSV。

### 3.8 场景与方法管理模块

场景卡片包含名称、适用任务、输入数据要求、当前版本和成果类型。业务人员选择场景后，系统加载相应参数表单与说明，展示可用数据和预期输出。已完成任务可作为该场景的案例，供其他项目参考其数据条件和成果组织方式。

技术人员在方法管理页面登记算法能力、维护 Skill 文档、编排步骤模板并配置成果字段。一个场景包发布后，系统同步更新场景卡片、参数表单、Agent 的 Skill 目录和结果展示配置。发布记录保存版本、说明、维护人及验证案例，历史任务继续关联原有方法版本。

### 3.9 项目记忆与经验管理模块

项目记忆保存常用研究区域、数据选择约定、关注对象及已确认的业务设置。用户可以查看、修改和删除这些记录，并选择适用项目。Agent 在新任务中按项目范围读取相关设置，减少重复输入。

经过复核的分析过程可整理为经验条目，记录适用区域、传感器、数据条件、参数选择依据与案例链接。技术人员可将稳定经验进一步写入 Skill 新版本。系统由此形成“使用场景—积累案例—复核经验—改进方法”的持续建设过程。

## 四、开发方案

### 4.1 总体技术路线

技术路线采用 React 地图交互、Python/FastAPI 业务服务、自研 Agent 与 OGE 在线计算相结合的架构。前后端管理项目、数据、任务和成果，Agent 通过 Tool 调用平台能力，Skill 提供场景方法，Memory 保存项目设置与案例经验。洪涝分析完成水体变化提取、承灾对象叠加及核查清单生成；灾后植被状态跟踪在同一项目中接入后续影像，开展指数计算、历史同期或未受灾对照与时序统计，结果表述为“持续异常筛查”而非因果性“恢复评价”。

实施顺序为：**数据与算子准备 → OGE 脚本及业务模型开发 → Python 服务与 React 工作台集成 → 自研 Agent 接入 → 示范案例验收。** 遥感脚本与业务模块同步开发，参数化任务完成计算、存储和展示联调后，接入 Agent 的需求解析、方法加载与工具执行。

### 4.2 数据来源与组织

#### 4.2.1 数据清单

| 数据 | 用途 | 拟采用来源和规格 | 接入与核查要求 |
| --- | --- | --- | --- |
| Sentinel-2 L2A 光学影像 | 光学水体提取、目视复核 | 优先检索 OGE 已收录产品；公开源可通过 Copernicus Data Space 获取 | 核实研究区、日期、表面反射率、质量层及产品缩放规则 |
| Sentinel-1 SAR 影像 | 云雨条件下的开放水面变化检测 | OGE 可用产品或经许可获取并完成预处理的外部产品 | 检查模式、相对轨道、升降轨、极化、辐射量及地形校正状态 |
| DEM | 地形辅助判读与坡度信息 | 平台可用 DEM 或公开的约 30 米 DEM | 统一覆盖范围、垂直单位、水平坐标和空值定义 |
| 耕地或土地覆盖 | 疑似受影响耕地统计 | 优先使用有授权的近年本地耕地数据；公开产品须记录版本和年份 | 记录分类体系、空间精度和数据年份，结合本地耕地分布核查 |
| 县乡行政区边界 | 裁剪与分区汇总 | 有权使用的行政区数据 | 记录区划版本，检查重叠、缝隙和行政代码 |
| 历史或基准水体 | 分离原有水体及识别季节性干扰 | 灾前多景有效观测，并参考可获得的历史水体产品 | 记录形成时间与水体频次，结合当期观测识别稳定及季节性水体 |
| 居民点、道路及重点设施 | 组织重点核查对象 | 有授权的业务数据或许可允许使用的公开数据 | 保留来源、更新日期及类别；缺少该类数据时明确该项分析未完成 |
| 降雨观测或格点资料 | 组织事件时间窗，辅助识别强降雨关注片区 | 核实 OGE 气象产品目录；接入有授权的气象站点或格点降雨资料 | 记录累计时段、时区、单位、缺测及空间分辨率，与影像时间轴关联 |
| 灾害事件与现场资料 | 建立过程背景和核查反馈 | 气象、应急部门事件资料及项目核查记录 | 保存来源、发生时间、位置和核实状态，与遥感结果关联 |

数据准备阶段按上述清单完成产品检索、影像覆盖核查与资产登记。平台数据通过产品及影像标识引用；外部数据经过许可、空间参考、格式和处理级别检查后，通过“我的数据”上传到 OGE。两类数据统一进入 OGE 分析流程，并以资产目录记录来源及处理信息。

#### 4.2.2 空间、时间与质量统一

光学影像以 Sentinel-2 L2A 为主要设计对象。其绿光 B3、近红外 B8 为 10 米波段，短波红外 B11 为 20 米波段，SCL 场景分类层可辅助识别云、阴影等像元。涉及 B11 的 MNDWI 主流程统一到 20 米分析网格，成果标注相应的分析尺度。[来源：Copernicus Sentinel-2 L2A 文档](https://documentation.dataspace.copernicus.eu/APIs/SentinelHub/Data/S2L2A.html)

连续反射率数据在网格对齐时采用合适的连续值重采样方法，类别数据和质量掩膜使用最近邻，防止类别值被插值改变。统一记录投影、网格原点、像元大小、栅格范围与 NoData。分类结果差分前检查影像对齐，避免水岸错位形成虚假的细条状新增水体。

灾前与事件期分别生成有效观测掩膜，二者交集作为变化分析的共同有效范围。研究区内的云、云影和无数据区域单独统计；多景拼接成果保存各分区的观测日期，支持按日期查看空间分布及变化。

#### 4.2.3 多源融合与 OGE 资源编排

平台围绕数据互补、分析协同和业务叠加组织多源融合。光学影像用于水体指数与地物判读，SAR 补充云雨条件下的开放水面观测；两类结果按实际观测时间和质量条件分别计算，在可比区域生成一致支持或待复核标记。DEM 派生坡度并辅助识别地形干扰，降雨数据用于描述事件进程和筛选时间窗口，耕地、道路与居民点将水面变化转化为承灾对象信息。

| OGE 资源层次 | 具体编排方式 | 保存的成果与业务用途 |
| --- | --- | --- |
| 数据 | 按区域与时间检索影像集合，登记气象、地形和承灾对象资产 | 可追溯的数据清单与时空覆盖，支持筛选分析条件 |
| 模型与算子 | 裁剪与对齐 → 质量掩膜 → 光学/SAR 分支 → 变化提取 → 空间叠加 → 分区统计 | 水面变化、质量标记、乡镇统计和核查对象 |
| 在线算力 | 具备独立输入的光学、SAR 和辅助数据步骤并行提交；依赖步骤按任务图衔接 | 记录排队、计算与成果生成时间，控制作业并发 |
| 结果衔接 | 中间结果通过平台引用传给下游，复用同一项目已完成的基准处理 | 减少数据重复传输和相同条件下的重复计算 |
| 应用服务 | 将完整分析模型绑定为场景服务，经 Tool 与 REST API 调用 | 同一计算链支持交互分析和外部系统接入 |

降雨数据按原始观测或格点尺度汇总累计量，计算时窗与事件记录一致；行政区统计保留站点覆盖或格点支持范围。气象数据与遥感提取结果通过区域和时间关联，支撑事件解释及重点片区选择。任务记录每种数据的尺度与用途，便于复核多源信息如何进入结论。

### 4.3 脚本开发（必选任务）

#### 4.3.1 脚本结构与输入输出

在 OGE 开发中心使用 Python 编写主脚本，采用 OGE 提供的 `oge.initialize()`、`oge.Service.initialize()`、`service.getCoverage()` 和 `service.getProcess(...).execute(...)` 组织数据读取与算子执行。开发中通过算子定义查询完成参数绑定与输入输出校验。

主脚本按数据准备、水体识别、变化提取、空间叠加和成果输出五个部分组织。可复用的子流程拆分为独立模块，共用统一的输入参数和结果字段，便于调试、服务封装与方法更新。脚本设计如下。

| 拟开发脚本 | 输入 | 输出 | 核心作用 |
| --- | --- | --- | --- |
| `洪涝影响综合分析.py` | 区域、影像引用、辅助图层、参数版本 | 成果目录与任务摘要 | 组织完整计算流程 |
| `光学水体变化.py` | 灾前及事件期光学数据、有效掩膜、水体参数 | 水体分类、变化分类、质量层 | 完成主要光学分析 |
| `雷达水体变化.py` | 预处理合格的同轨 SAR 影像对、地形辅助层 | 雷达候选积水与质量标记 | 提供云雨条件下的观测结果 |
| `影响叠加统计.py` | 积水候选、有效范围、耕地、区划和重点对象 | 乡镇统计及空间对象清单 | 将遥感结果转换为业务信息 |

任务输入至少包括 `aoi`、灾前及事件期数据引用、分析网格、输入质量说明和方法参数。阈值、最小图斑面积、邻域滤波尺度及核查规则均以有单位、有范围的配置项管理。任务进入计算前统一完成参数校验，建立业务描述与计算输入之间的明确对应。

输出至少包括灾前水体、事件期水体、疑似新增积水、有效观测范围、候选积水矢量、乡镇统计和元数据说明。每项输出记录对应 `processId`、数据引用、坐标系、单位、生成时间及结果版本。

#### 4.3.2 OGE 算子组合与应用

| 处理环节 | 文档中的能力或算子 | 本作品中的用途 |
| --- | --- | --- |
| 数据读取 | `Service.getCoverage`、`Service.getCoverageCollection`、`Feature.load` | 读取平台影像、集合与矢量数据 |
| 波段与数值准备 | `Coverage.selectBands`、`Coverage.toFloat` | 选择实际波段，避免整型运算影响指数结果 |
| 网格与范围统一 | `Coverage.reproject`、`Coverage.warpByGDAL`、`Coverage.clipRasterByMaskLayerByGDAL` | 投影、网格对齐与研究区裁剪 |
| 指数计算 | `Coverage.normalizedDifference` | 按实际波段计算 NDWI、MNDWI |
| 阈值分类 | `Coverage.binarization`、`Coverage.gt`、`Coverage.lt` | 按已校准阈值形成水体候选 |
| 差分 | `Coverage.subtract` | 对齐后的灾前、事件期分类差分 |
| 邻域处理 | `Coverage.focalMedian`、`Coverage.focalMin`、`Coverage.focalMax` | 控制孤立噪声与局部形态处理 |
| 掩膜 | `Coverage.mask` | 应用有效观测及质量约束 |
| 地形辅助 | `Coverage.terrSlope` 等坡度算子 | 提供地形判读信息 |
| 栅格转矢量 | `Coverage.polygonizeByGDAL` | 提取连通候选图斑 |
| 空间叠加 | `Feature.intersection`、`Feature.difference`、`Feature.buffer` | 耕地、区划与重点对象分析 |
| 几何统计 | `Feature.area`、`Feature.length` | 计算面积和相关道路长度 |
| 点位采样 | `Feature.rasterSamplingByQGIS` | 提取重点点位对应的分类信息 |
| 展示与输出 | `styles(...).getMap(...)`、文档中的导出能力 | 在 OGE 开发中心展示并输出结果 |

洪涝处理链依次完成事件时间匹配、有效观测筛选、水体指数与阈值分类、前后期差分、雷达交叉核查以及耕地和重点对象叠加。水体变化结果按乡镇汇总，并关联核查图斑与对象清单。相关水体指数、二值化、差分和点位采样调用参考 OGE 应用开发文档第 4.1 节。

开发第 1 周建立算子能力与服务发布清单，逐项完成脚本调用、接口权限、参数类型和输出格式核查。二值化、掩膜等关键步骤先采用小范围样例验证输出编码及 NoData 处理，再纳入主流程；通过验证的算子统一登记到业务工具层。

#### 4.3.3 光学水体提取

光学路径采用经过云影处理的表面反射率影像，以改进归一化差异水体指数 MNDWI 为主要判别变量，并保留 NDWI 作为对照。

\[
MNDWI = \frac{\rho_{Green}-\rho_{SWIR1}}{\rho_{Green}+\rho_{SWIR1}}
\]

\[
NDWI = \frac{\rho_{Green}-\rho_{NIR}}{\rho_{Green}+\rho_{NIR}}
\]

对于拟用的 Sentinel-2 L2A，MNDWI 使用 B3 与 B11，NDWI 使用 B3 与 B8。系统按产品配置维护波段对应关系。指数采用归一化差异形式的依据包括 McFeeters 的 NDWI 研究及 Xu 的 MNDWI 研究。[NDWI 原始论文](https://doi.org/10.1080/01431169608948714)，[MNDWI 原始论文](https://doi.org/10.1080/01431160600589179)。由于 B11 原始分辨率为 20 米，MNDWI 主成果的有效信息尺度按 20 米说明；窄河道、小微积水和道路级判定只作为核查线索，不报告超出数据尺度支持的精细结论。

处理前按产品元数据完成反射率转换，处理分母为零与无效值。水体阈值通过独立校准样本确定，样本覆盖水体、湿土、建筑阴影和水田等典型地物。校准后的阈值与方法版本共同保存，作为业务任务的计算参数。后续可结合直方图分布研究自动阈值方法，并通过对照验证完善其适用范围。

分类后的邻域处理以保持河道和小型积水形态为原则。最小图斑面积以平方米配置，并转换为当前网格的像元数；滤波前后均保留结果用于比较。窄河道、田埂附近和建筑密集区域列为重点误差检查对象。

#### 4.3.4 雷达水体变化路径

SAR 路径用于补充汛期云雨条件下的水面观测，以 Sentinel-1 数据为主要输入。数据准备包含噪声处理、辐射定标、地形校正及斑点处理，并统一后向散射量和单位。各产品的预处理步骤与参数进入资产目录，为双时相比较提供一致的数据基础。[来源：Copernicus Sentinel-1 GRD 文档](https://documentation.dataspace.copernicus.eu/APIs/SentinelHub/Data/S1GRD.html)

SAR 脚本读取完成预处理的 GeoTIFF。输入准备优先使用平台中处理级别满足要求的数据；外部 GRD 由团队参考 [ESA 洪水制图教程](https://step.esa.int/docs/tutorials/tutorial_s1floodmapping.pdf)，在 ESA SNAP 中按固定参数完成处理后上传。OGE 负责后续水体判别、变化提取、空间叠加和面积统计，各阶段处理信息随数据一并登记。

灾前与事件期影像应具有可比较的观测几何，优先采用相同相对轨道、升降轨方向、获取模式和极化的数据。对已校正并统一网格的后向散射，利用事件期低散射特征和相对灾前的变化共同识别开放水面候选。若采用分贝值，变化量定义为：

\[
\Delta\sigma^0_{dB}=\sigma^0_{event,dB}-\sigma^0_{pre,dB}
\]

计算前将灾前与事件期数据统一为相同辐射量及分贝单位，按照上述差分定义提取变化特征。低散射与变化阈值由本地校准样本确定，并记录到参数版本中。

雷达水体识别结合地形阴影、叠掩等质量标记开展筛选，DEM 坡度用于辅助判读。本期低散射方法重点用于开放水面变化；林下淹水、建筑区双次散射及风浪影响区域纳入专项复核，结合光学观测与现场资料完善判断。

光学和 SAR 结果分别保存实际观测时间与有效范围。交叉核查前在场景版本中预设最大观测时差、空间容差、最小共同有效面积和冲突处理规则；只有在时间条件、共同覆盖与分类口径均满足时，才标记“多源一致支持”。其他情况分别标记单源支持、结果冲突或观测不足。多日期结果按时间序列展示，仅表征卫星过境时刻的观测状态，不默认代表洪峰时刻的最大淹没范围。

#### 4.3.5 水体变化与候选积水形成

设灾前水体为 \(W_{pre}\)，事件期水体为 \(W_{event}\)，二者共同有效观测区域为 \(V\)，则新增水体候选定义为：

\[
F_{candidate}=(W_{event}\setminus W_{pre})\cap V
\]

在此基础上，结合历史水体、地形质量、水田灌溉和人工复核信息整理候选结果。稳定基准水体单列显示，具有季节变化的河滩、库区及水田作为重点复核对象，保留其变化图斑与判读依据。

计算中统一水体编码。若平台二值化输出为 0/255，差分前需确保使用能够容纳负值的数值类型，防止无符号整型下溢；随后将新增水体、退水、未变化和无效观测明确映射为不同类别。无效像元通过质量掩膜单独管理。

成果以影像实际观测时段描述水面变化。结合事件时间线，标注卫星过境与主要降雨过程的先后关系，并记录灾前水体状态，使用户能够判断本次分析反映的是积水发展、持续还是消退阶段。

#### 4.3.6 耕地叠加与面积统计

将候选积水矢量与耕地范围、行政区边界相交，形成乡镇和图斑级统计。面积计算采用适合研究区的等积投影，并统一栅格与矢量的统计范围和单位。Web 地图展示与面积计算分别使用对应的空间参考，统计结果通过图斑抽查和栅格面积汇总进行交叉核验。

对乡镇 \(i\)，设其范围为 \(T_i\)，耕地为 \(C\)，采用的候选积水为 \(F\)，有效观测范围为 \(V\)，则：

\[
A_i=Area(F\cap C\cap T_i),\qquad
R_i=\frac{Area(F\cap C\cap T_i)}{Area(C\cap V\cap T_i)}\times100\%
\]

其中，\(A_i\) 为疑似受影响耕地面积，\(R_i\) 为其占**有效观测耕地面积**的比例。分母为零时显示“无法计算”。同时输出总耕地面积、有效观测耕地面积与观测覆盖率，共同描述各乡镇的观测覆盖和影响分布。

统计前检查行政区和耕地图斑重叠，避免重复累计。公顷与平方公里分别使用 10,000 平方米、1,000,000 平方米换算；显示精度与数据尺度匹配，后台保存原始数值。公开土地覆盖年份早于事件年份时，在表格与报告中说明时效差异。

### 4.4 巡查优先级与证据表达

巡查优先级围绕基层核查工作的安排需求设计，以重点对象关联、耕地重叠和积水规模作为排序依据。各指标直接展示原始数值与空间关系，使业务人员能够了解排序原因，并结合工作重点调整核查顺序。

排序单元优先采用乡镇内部去重后的候选积水图斑。首先依据是否关联重点设施或居民点进行分组，再依次参考疑似受影响耕地面积、候选积水面积和关联道路长度排序；同值时使用稳定的图斑编号确定展示顺序。重点设施与居民点按数据类别配置业务规则。数据缺项以状态字段记录，排名限定在指标口径一致的任务范围内。

| 清单类别 | 判定依据 | 呈现与建议 |
| --- | --- | --- |
| 优先核查 | 候选积水与已知重点设施或居民点相交，或进入事先设置的邻近范围 | 显示关联对象及空间关系，优先核实现场影响 |
| 耕地重点核查 | 未进入上一类，但与耕地重叠 | 按重叠面积等原始指标排序，核实是否为正常灌溉及作物实际状态 |
| 一般积水核查 | 其余可观测候选图斑 | 查看分布、规模和周边关系，纳入常规核查 |
| 观测补充清单 | 重点对象周边无有效观测，或质量冲突影响判断 | 结合业务重要性安排影像补充或现场核查 |

邻近范围以米为单位配置，清单分别展示相交对象与邻近对象，并注明距离和空间关系。图斑证据分为“多源一致支持、单源有效支持、结果冲突、观测不足”四类，其中多源一致性依据可比时段的观测评定。证据状态与业务优先级分别展示，使观测较少但涉及重要对象的片区也能得到及时关注。

### 4.5 服务发布与 OGE OpenAPI 集成

#### 4.5.1 服务化方式

服务开发基于已验证的脚本逻辑，将水体识别、变化分析和影响统计封装为算子或组合模型，经过平台服务注册与发布配置后接入 OpenAPI。发布阶段完成输入输出定义、参数绑定、权限配置和调用联调，形成可供智能体使用的服务目录。

计算服务围绕稳定的中间成果拆分，分别定义水体栅格、变化图层和统计结果的类型及字段。水体识别完成后启动变化分析，再由变化结果触发影响统计。多输出组合模型登记成果名称和选择方式，工具层据此将对应结果传递给后续步骤。

完整案例同时封装为本平台的 WebService：接收区域、事件窗口、输入资产与场景版本，返回任务编号、状态查询地址及成果地址。内部将数据检查、OGE 模型执行、影响统计和成果登记组织成稳定流程。外部业务系统提交相同请求即可获得图层、表格、核查清单和报告，交互工作台与 Agent 共用这一流程。

#### 4.5.2 接口对应关系

以下接口统一使用 OGE 网关 Base URL 配置，按数据准备、计算执行和成果读取三个环节组织。

| 工具能力 | 手册接口 | 使用说明 |
| --- | --- | --- |
| 平台登录 | `POST /oauth/token` | 后端获取并管理用户凭证 |
| 上传与资产确认 | `POST /asset/myData/upload`、`POST /asset/myData/list` | 上传后查询资产列表，登记资产标识 |
| 产品发现 | `POST /data-product/page` | 遍历分页，记录产品名称与编号 |
| 影像发现 | `GET /data-product/image/listByProductId` | 读取影像元数据并进一步核对时空覆盖 |
| 算子参数查询 | `GET /computation-api/process/info` | 按 `processName` 查询输入及输出字段 |
| 原子算子执行 | `POST /openapi/algorithm/{包}/{算子}/execute` | 请求体为算子参数，使用 AppKey |
| 组合模型执行 | `POST /openapi/combination/{serviceName}/execute` | 调用已发布模型，使用 AppKey |
| 执行状态 | `GET /computation-api/process/{processId}` | 读取执行状态、消息及结果元数据 |
| 结果信息 | `GET /computation-api/process/result/{processId}` | 读取结果类型与路径 |
| 成果下载 | `GET /computation-api/process/result/download/{processId}` | 带鉴权读取文件流 |
| 栅格预览 | `GET /computation-api/styles/cog/{processId}` | 手册定义为图片流，用于结果预览 |

数据上传成功后，后端通过资产列表获取 `assetUuid` 并建立数据引用。影像发现采用产品分页与影像列表相结合的方式，进一步利用返回元数据和栅格空间信息筛选时间、范围及质量，形成进入分析的最终数据清单。

手册定义了平台单景引用 `Platform:Coverage:{imageIdentification}:{productName}`、个人资产引用 `Personal:Asset:{assetUuid}` 和前序结果引用 `Personal:Process:{processId}`。本作品据此构建任务依赖：只有前序任务成功且结果类型匹配，才将其引用传给后续步骤。这种方式能在平台内部衔接处理结果，减少在应用后端重复下载和上传栅格。

#### 4.5.3 鉴权与协议联调

平台 JWT 和 AppKey 由后端凭证模块统一管理，业务工具按接口配置注入认证信息。网关地址、鉴权方式、许可算子和访问白名单集中配置，日志对敏感字段脱敏，浏览器通过业务后端访问任务和成果。

接口联调覆盖登录参数、状态查询认证、业务状态码及结果结构，并将平台环境中确认的协议固化到接入配置。响应校验同时检查 HTTP 状态、业务状态和必要结果字段，按鉴权、参数、服务和计算问题分类处理。

输出文件名采用可读的“区域_事件日期_任务序号_步骤_版本”形式，例如 `榕江县_20250624_任务001_新增积水_v1.tif`。提交前由数据库分配任务序号，满足平台同一用户输出名称不重复的要求，便于按地区、事件、步骤和版本检索成果。

### 4.6 自研 Agent 架构与能力管理

#### 4.6.1 总体设计及组件关系

Agent 采用 Python 自主实现，参考 DeepSeek Harness “everything is a plugin”、能力接缝、类型事件和可恢复会话日志的设计思路，但不复制其 TypeScript/Cordis 实现。本作品将运行底座收缩为“插件容器 + 生命周期事件 + 追加式会话日志”；模型适配、工具注册、Skill 加载、Memory 存储、执行策略和交互呈现均以有明确接口和作用域的插件挂载。OGE 能力不写入 Agent 循环，而由 Tool 提供者、算法绑定和 GeoSkill 场景包协同提供。

借鉴关系以本地阅读的 DeepSeek Harness 版本 `5dda764ed3` 为设计参考，仅用于说明架构思路来源。团队交付的 Python 运行底座、OGE 异步任务适配、空间上下文引用、遥感质量门禁和 GeoSkill 场景契约由团队自行设计实现。答辩材料将附第三方依赖和许可证清单，明确“参考架构”与“自研代码”的边界。

![图4 Agent与场景扩展架构](方案配图/图4_Agent与场景扩展架构.png)

**图 4 说明：** Agent 运行内核读取任务目标，加载适用 Skill 和项目 Memory，选择 Tool 形成或执行分析计划。Tool 调用项目、数据、任务、成果等业务服务，任务调度器据此访问 OGE。执行结果回到运行内核，同时登记到业务存储与执行轨迹。经复核的经验可形成 Memory 条目，并进一步整理为 Skill 新版本。

各部分围绕明确的对象分工。

| 对象 | 解决的问题 | 洪涝场景中的例子 | 主要维护方式 |
| --- | --- | --- | --- |
| OGE 算法服务 | 对空间数据完成何种计算 | 水体提取、变化检测、耕地叠加 | 遥感人员编写脚本、校准参数并发布服务 |
| Tool | 系统能够执行哪些具体操作 | 查找影像、提交工作流、查询图斑统计 | Python 接口与处理函数，登记结构化参数 |
| Skill | 一类业务任务如何组织和解释 | 选择灾前基准、组合水体与耕地分析、检查质量 | 场景文档、流程模板、规则及案例，版本化发布 |
| Memory | 当前项目和历史工作中有哪些可用上下文 | 研究区、常用耕地图层、已复核案例 | 按范围保存、检索、更新及失效处理 |
| Agent 运行底座 | 如何挂载能力、推进回合并保存事实 | 检索数据后确定分析分支，收到成果事件后继续任务 | 自研插件容器、状态机、事件日志与上下文组装 |
| 业务任务与成果 | 一次分析的进度和产物如何保存 | 任务步骤、OGE 作业编号、地图、统计和报告 | Python 业务服务与持久化存储 |

Tool 提供操作入口，Skill 组织这些操作，Memory 提供当前任务相关的信息，OGE 算法产生空间结果。业务系统将它们转化为用户可以持续操作的项目和成果。

#### 4.6.2 Python 组件接口与装配

应用启动时根据 Profile 组装插件树，按依赖顺序完成注册，Agent 以一次持久会话运行作为单位。每个插件通过 `PluginManifest` 声明编号、版本、Host API 版本、能力、依赖、权限和配置 Schema，并实现可逆的 `activate/deactivate` 生命周期。主要能力接口设计如下。

| 自主设计的接口 | 主要职责 | 输入与输出 |
| --- | --- | --- |
| `ModelAdapter` | 请求模型、读取流式响应和工具调用 | 上下文与工具定义 → 消息、工具调用请求、用量 |
| `ToolRegistry` | 登记与检索当前可用工具 | 工具名称和版本 → 参数定义、处理函数及调用策略 |
| `SkillManager` | 检索场景方法、读取指定版本、解析关联资源 | 业务目标、场景编号 → Skill 内容与依赖 |
| `MemoryStore` | 分范围检索、保存和更新记忆 | 项目、类型、条件 → 带来源的记忆条目 |
| `ContextBuilder` | 整理本轮模型上下文 | 当前目标、Skill、Memory、执行摘要 → 请求上下文 |
| `AgentRunner` | 推进模型与工具交互，管理运行状态 | 会话及用户请求 → 执行事件、任务关联和回复 |
| `PolicyGate` | 按作用域审查工具调用与需要人工确认的副作用 | 调用主体、工具、参数和上下文 → 允许、拒绝或待确认 |
| `WorkflowService` | 创建、校验和执行任务图 | 输入资产、步骤依赖、参数 → 任务及成果 |
| `SessionEventStore` | 追加保存回合、步骤、工具和任务事件 | 运行事件 → 可重放的会话与可查询轨迹 |

组件通过 Python Protocol 定义协作接口，Pydantic 模型统一描述请求、工具参数、结果和运行事件。会话日志是模型可见历史和恢复状态的单一事实源；进入模型上下文的项目、Skill、Memory 和工具结果必须能由持久事件重建。OGE 与模型适配器通过 HTTPX 管理 HTTP 调用，Agent 使用 asyncio 组织等待、时限与取消信号。长时间 OGE 作业与会话进程分离，由独立 Worker 跟踪并以持久事件唤醒后续运行。

#### 4.6.3 Tool：通用操作与算法能力绑定

**OGE 的数据与计算接口通过 Python 适配器封装为 Agent Tool。** 例如，`data.search` 对接 OGE 产品与影像接口，`algorithm.describe` 读取算子定义，`workflow.submit` 根据算法绑定提交 OGE 原子算子或组合模型，`task.get_status` 和成果工具读取平台作业状态与产物。Agent 调用的是具有业务名称、类型约束和执行记录的工具；工具内部完成实际 OGE 请求。

工具体系按业务对象划分，兼顾 Agent 调用和普通页面操作。工具读取或修改业务对象，返回结构化结果；浏览器中的同类操作使用对应业务服务。

| 工具组 | 计划定义的工具 | 主要输入 | 主要输出 |
| --- | --- | --- | --- |
| 项目与空间范围 | `project.get_context`、`region.resolve` | 项目、行政代码或绘制区域 | 研究区、关注对象和当前设置 |
| 数据发现 | `data.search`、`data.describe` | 区域、时间、产品及质量条件 | 候选资产、覆盖、波段和采集信息 |
| 方法发现 | `skill.search`、`skill.load` | 业务目标或 Skill 编号与版本 | 场景摘要、输入定义、方法和资源引用 |
| 算法目录 | `algorithm.search`、`algorithm.describe` | 逻辑能力、输入类型或服务编号 | 参数定义、结果类型和可用状态 |
| 任务规划与运行 | `workflow.plan`、`workflow.submit`、`task.get_status` | 场景版本、资产引用、参数和步骤 | 计划、任务编号及节点状态 |
| 成果读取 | `result.list`、`result.describe`、`result.query` | 任务、成果、区域或图斑编号 | 成果元数据、局部统计和质量摘要 |
| 核查与报告 | `review.create`、`report.generate` | 图斑、复核意见或成果版本 | 核查记录、报告编号与下载地址 |
| 记忆使用 | `memory.search`、`memory.propose` | 项目范围、检索条件或候选经验 | 相关条目或待复核记忆记录 |

表中名称为本系统的工具接口设计。每个工具登记名称、说明、版本、输入与输出 JSON Schema、权限范围、读写类型、同步或异步方式、超时时间及处理函数。只读查询经参数校验后执行；创建正式任务、修改核查记录、发布 Skill 和将经验写入长期记忆属于有副作用操作，必须通过 `PolicyGate` 并获得用户确认或预先配置的业务授权。任务提交返回业务 `taskId` 和执行状态，OGE `processId` 由任务服务保存，业务请求编号用于幂等受理。

模型不能直接拼接 OGE 地址、SQL 或服务器文件路径，不持有 JWT、AppKey 或模型凭证。外部文档、资产元数据、Skill 文本、Memory 和工具返回均按不可信输入处理：后端仅接受白名单资源引用与结构化参数，并以项目级 ACL、最小权限、输出脱敏和操作日志防止越权及指令注入扩大影响。

工具统一返回业务状态、结构化数据、引用对象、事件编号及错误信息。数据检索结果可返回资产列表，计算结果主要返回成果标识和摘要，大文件通过成果服务访问。界面根据工具返回的展示类型渲染影像卡片、计划卡片、统计表或报告链接，用户能够看到操作产物。

**算法服务通过独立目录绑定。** 目录记录逻辑能力、实现版本、OGE 服务类型和名称、输入输出定义、支持的数据类型、单位及服务状态。Skill 使用逻辑能力组织步骤，任务创建时解析并固定具体实现。以洪涝为例，可定义“光学水体识别”“双时相变化”“耕地叠加统计”等能力；服务名称和参数映射在 OGE 发布联调时登记。

初版通过一组通用工具操作算法目录和工作流。增加同类型遥感算法时，主要更新算法登记、参数映射与场景配置；新增工具用于接入新的业务操作或数据类型。例如，增加新的水体识别算法可继续使用工作流提交工具，增加外业照片定位业务则需要对应的资产及查询处理能力。

#### 4.6.4 Skill：可复用的遥感业务方法

Skill 以一类完整分析任务为单位组织，包含业务目的、适用条件、数据选择、处理步骤、质量检查和成果解释。系统将说明文字与可执行配置共同保存在一个版本化场景包中。

```text
scenarios/
  flood-assessment/
    manifest.yaml          场景编号、版本、输入、输出和依赖
    SKILL.md               业务目标、数据选择、处理策略与解释方法
    workflow.yaml          步骤、依赖、条件分支及参数映射
    validation.yaml        输入、质量和成果完整性规则
    presentation.yaml      表单、图层样式、统计列与报告字段
    references/            算法依据与判读说明
    examples/              经过验证的任务配置与案例说明
```

其中，`SKILL.md` 提供 Agent 需要理解的专业方法；`workflow.yaml` 将主要计算步骤表示为可执行任务图；`validation.yaml` 定义可以自动检查的条件；`presentation.yaml` 决定用户看到的参数表单、图层和统计。`manifest.yaml` 关联以上资源，并标明场景依赖的 Tool 和算法能力。Python 场景服务解析这些配置，React 根据输入和展示定义生成相应界面。

场景包遵循明确的扩展契约。`manifest.yaml` 除业务输入输出外，还声明 `apiVersion`、宿主版本约束、能力编号、依赖、权限和成果类型。生命周期为“发现—结构校验—依赖与权限检查—小样本验证—发布—启用—停用/回滚”。任务创建时锁定场景版本与具体 OGE 实现，升级不改写历史任务。运行时只允许访问声明的 Tool 与项目资源，单个场景加载或执行失败不影响其他场景。

“可插拔”的边界为：符合已有资产、栅格、矢量、表格和报告契约的 OGE 能力，可通过算法绑定和场景配置低代码接入；新的数据模态、成果类型或专用交互仍需增加宿主适配插件。方案不宣称任意 OGE 功能均能零代码接入。

洪涝 Skill 的核心内容包括：如何选择具有代表性的灾前基准，如何检查光学有效覆盖和 SAR 预处理条件，何时组织单源分析或多源核查，如何处理基准水体与耕地季节性用水，如何计算有效观测范围内的影响面积，以及如何将图斑整理为核查清单。阈值、最小图斑面积和邻近距离进入参数配置，参数选择依据写入方法说明。

场景清单的设计示例如下。能力编号属于本系统的逻辑定义，发布场景时与算法目录中的具体 OGE 服务绑定。

```yaml
id: flood-assessment
version: "1.0"
name: 洪涝影响分析
inputs:
  region: geometry
  baseline_assets: asset_list
  event_assets: asset_list
  cropland: asset
outputs:
  water_change: raster
  affected_cropland: vector
  regional_statistics: table
  inspection_list: table
requires:
  tools: [data.search, algorithm.describe, workflow.submit, result.query]
  capabilities: [water.extract, change.detect, land.overlay_statistics]
resources:
  instructions: SKILL.md
  workflow: workflow.yaml
  validation: validation.yaml
  presentation: presentation.yaml
```

**Skill 按需加载。** Agent 首先读取场景名称、用途、输入和适用条件等摘要，结合用户指定场景或业务目标选出相关 Skill，再加载正文和本次任务需要的参考资源。选择结果在任务卡片中展示。影像波段、数据时相等必要条件交给规则校验，数据满足后再生成可执行计划。

**Skill 按版本沉淀。** 方法维护流程为“草稿—案例验证—技术复核—发布”。发布时固定场景资源、算法实现和成果定义，保存维护说明与验证案例。历史任务记录使用的版本，新版本用于后续任务；需要比较方法差异时，由同一数据分别运行两版场景。

#### 4.6.5 Memory：会话、项目与经验的分层记忆

Memory 主要用于连续工作和经验复用，按生命周期及作用范围分为四类。

| 记忆层 | 保存内容 | 写入时机 | 使用方式 |
| --- | --- | --- | --- |
| 会话工作记忆 | 当前目标、所选区域、活动图层、待补参数、计划和工具结果摘要 | 用户操作和每轮工具返回后更新 | 组织下一轮请求，理解“这里”“上一期”等指代 |
| 项目长期记忆 | 研究区、常用资产、关注对象、报告设置及已确认的数据约定 | 项目设置变更或用户保存后写入 | 新任务初始化、数据筛选与报告配置 |
| 案例过程记忆 | 任务输入、Skill 与算法版本、参数、成果引用、质量和复核结论 | 任务完成及复核后形成摘要 | 查找类似任务，比较方法和处理经验 |
| 方法经验记忆 | 经技术复核的参数适用条件、易混淆地物及处理建议 | 从案例整理并复核后发布 | 为相关任务提供有来源的方法参考，支持 Skill 修订 |

记忆条目保存 `memoryId`、类型、项目范围、内容摘要、结构化属性、来源引用、适用条件、创建与更新时间、复核状态、有效状态和版本。可选适用条件包括区域、传感器、产品级别、季节、算法版本及业务场景，便于判断历史经验与当前任务是否相关。

**检索过程。** 先按用户权限和项目范围过滤，再依据当前场景、区域、传感器、时间及版本匹配条目，最后按相关性、复核状态和更新日期排序。首版采用 PostgreSQL 结构化条件和中文分词关键词索引，返回少量高相关摘要及来源。Agent 需要详细参数或数值时，通过资产和成果工具读取对应业务记录。

**写入与更新。** 会话状态随交互保存，项目设置随明确的业务修改更新，任务完成后自动形成可检索的案例摘要。Agent 可以提出候选经验，技术人员复核后将其作为方法经验发布。任务成果和核查意见是经验的来源，长期记忆保留这些引用，方便后续修改与复查。

**时效与冲突处理。** 当前任务参数优先于项目默认设置，项目设置优先于历史经验建议。资产更新、方法版本变更或复核结论修订时，关联条目进入重新评定；已失效条目保留历史关联并退出当前推荐。用户在项目记忆面板可查看、修改、停用或删除条目。

例如，榕江县项目可以记住常用乡镇边界、已确认的耕地图层及重点关注区域；某次水体阈值的使用经验则同时记录传感器、反射率处理方式、观测季节、算法版本和验证结果。下一次任务读取这些信息后，按当前数据条件决定是否采用相关建议。

#### 4.6.6 上下文组装与执行循环

一次会话运行先获得项目与页面选择，再按业务目标匹配 Skill、检索 Memory 并加载本轮需要的 Tool 定义。模型上下文由以下部分组成：任务目标与参数、当前场景方法、相关记忆摘要、可用工具、最近交互、任务状态和必要成果摘要。完整影像、表格和历史事件保存在业务存储中，通过引用按需读取。

`ContextBuilder` 按配置分配上下文容量：当前目标、输入条件、Skill 版本和执行状态作为固定信息；参考资料与历史记忆按相关性截取；较早会话整理为带来源的摘要。模型请求保留本轮实际加载的 Skill、记忆及工具版本，方便分析工具选择与结果解释的依据。不可信文本与系统策略、工具 Schema 分层组装，资产描述或历史记忆中的指令不能改写权限、工具集或执行规则。

运行过程按“理解目标—发现方法—补齐数据—生成计划—执行工具—接收结果—继续分析或结束”推进。运行状态包括准备、规划、工具执行、等待任务、结果整理、完成和待处理。计划建立时校验步骤依赖、输入输出类型、算法可用性与参数范围，并固定场景和服务版本。

OGE 长时间作业由调度进程与 Python Worker 共同跟踪。工作流提交工具返回任务编号后，Agent 保存检查点并进入等待状态；步骤完成或异常时，持久事件触发运行内核继续处理。页面关闭后任务状态仍保存在后台，重新进入项目时可恢复关联。只读查询、成果筛选和解释在已有任务上完成，需要新的空间计算时建立新的任务或版本。

当中间结果显示覆盖需要补充、分类存在差异或参数需要调整时，Agent 依据 Skill 的分支规则提出后续计划。原计划与调整原因均留在任务记录中。模型调用按轮次、时间和用量设置预算，任务调度按并发数和平台能力安排作业，分别管理交互成本和计算资源。

#### 4.6.7 从一次操作到方法沉淀

以用户在地图上选择某乡镇并提出“分析这里的耕地积水，沿用上次的数据设置”为例，系统执行过程如下。

1. 页面将项目编号、所选区域、活动成果和用户文本传给会话服务。
2. Memory 服务读取该项目的数据约定与上次任务引用，Skill 管理器加载洪涝场景的指定版本。
3. Agent 调用数据与成果工具核对本次可用影像、时相和耕地资产，形成分析计划。
4. 用户在计划卡片中查看范围、输入、步骤和参数，提交后由工作流服务生成任务图。
5. 调度进程安排步骤，Python Worker 调用 OGE 完成水体变化和叠加统计，登记成果并向页面推送事件。
6. Agent 读取结果摘要，结合 Skill 的解释规则说明主要分布和核查重点，地图同步加载相关图层。
7. 用户完成图斑复核并保存报告，系统将此次任务整理为带来源的案例过程记忆。
8. 技术人员将多次案例中稳定的处理经验汇总到 Skill 草稿，经验证发布后供其他项目复用。

执行记录包含 `projectId`、`sessionId`、`runId`、`taskId`、步骤编号、工具版本、Skill 版本、算法绑定、记忆引用、事件时间和成果标识。这些关联使用户能够从一句操作请求追查到实际计算和最终产物，也使后续方法沉淀具有明确依据。


### 4.7 异步任务、数据记录与报告一致性

FastAPI 任务接口为每个任务分配 `taskId`，为每个步骤保存依赖与 OGE `processId`。任务状态包括待检查、待执行、运行中、待补充信息、成功、失败和已停止编排。PostgreSQL 保存任务图、步骤、执行租约与事件；Celery Worker 负责领取就绪步骤、提交作业、查询状态和登记结果，Redis 承担队列传输。OGE 执行遥感计算，Worker 按间隔发起短时状态查询并保存下次检查时间。

任务与待发送事件在同一数据库事务内保存，由调度进程投递队列。Worker 根据步骤状态和执行租约领取任务，重复消息复用已有记录。服务启动后扫描到期待查询作业与未投递事件，恢复后续处理。Agent 的会话运行同样通过任务事件推进，API 请求在任务受理后返回，浏览器通过 SSE 和任务查询持续获取进度。这一部署方式采用独立后台任务进程组织持久执行。[FastAPI 后台任务文档](https://fastapi.tiangolo.com/tutorial/background-tasks/)，[Celery 任务文档](https://docs.celeryq.dev/en/stable/userguide/tasks.html)

“停止编排”控制后续步骤的提交，已进入 OGE 的作业继续由后端跟踪。服务重启或网络恢复后，后端依据保存的 `processId` 恢复状态查询。作业完成时校验成果文件、类型和元数据，确认结果可读取后更新业务步骤状态。

任务提交使用唯一业务请求编号管理重复请求。提交状态不明确的作业进入待核对队列；明确失败后的重新运行生成新的步骤记录与输出名称。状态查询设置调用间隔、次数和时限，超出时限的任务转入待处理状态，并保留原始响应供定位问题。

业务数据库拟使用 PostgreSQL，并启用 PostGIS 保存研究区、图斑及对象几何，存储以下内容。

| 记录 | 主要字段 | 作用 |
| --- | --- | --- |
| 任务 | 区域、事件时间、关注对象、版本、状态 | 确定本次分析的范围 |
| 输入资产 | 来源、产品编号、影像标识、日期、质量、许可说明 | 追溯实际使用的数据 |
| 执行步骤 | 服务名、参数、依赖、`processId`、起止时间、状态 | 复查计算过程 |
| 成果 | 类型、路径、投影、单位、有效范围、所属步骤 | 统一管理地图与统计 |
| 核查标记 | 图斑编号、负责人、处理状态、现场描述、附件、复核人和时间 | 保存工作安排、反馈和人工复核依据 |
| 报告 | 对应结果版本、引用字段、生成时间 | 保证文字与数字一致 |
| 智能辅助会话 | 会话编号、消息、工具调用、关联任务及用量 | 管理 Agent 上下文与执行记录 |
| 项目 | 研究区、设置、成员范围、活动任务及成果 | 组织持续分析的工作空间 |
| 灾害事件 | 事件类型、时间窗口、资料、关注对象及关联任务 | 组织同一区域的多次灾害过程与后续观测 |
| 场景及版本 | Skill 资源、步骤模板、算法绑定、表单及成果配置 | 加载场景与追溯方法 |
| 算法目录 | 逻辑能力、OGE 服务、参数映射、结果类型及版本 | 将业务方法连接到计算实现 |
| 记忆与来源 | 类型、范围、摘要、适用条件、来源、复核及有效状态 | 检索与管理项目设置和方法经验 |

PostgreSQL 保存业务记录与持久事件，PostGIS 支持成果几何的查询和定位；正式遥感计算与空间汇总在 OGE 完成。栅格与空间文件存入独立成果目录，数据库登记访问引用和版本。自研 Agent 的会话、工具执行记录与业务任务通过编号关联，常规业务操作与智能辅助操作统一记录操作者、时间和成果版本。后端向模型传递当前任务所需的元数据、统计和质量摘要。

Python 报告服务先整理结构化事实表，再按模板组织图表与正文。Agent 可以依据成果生成解释摘要，将摘要与成果引用一起交给报告服务；报告服务校验数值、单位和引用后完成编排与导出。结果更新后生成对应版本的报告，历史报告保留原有成果引用，保持图、表、文一致。

### 4.8 Web 开发与运行环境

#### 4.8.1 前后端接口与地图处理

Python 后端按项目、场景、数据、任务、成果、核查、报告和智能辅助组织业务接口。主要接口设计如下。

| 接口 | 作用 |
| --- | --- |
| `GET/POST /api/v1/projects` | 查询或创建项目工作空间 |
| `GET /api/v1/scenarios` | 查询可用场景及摘要 |
| `GET /api/v1/scenarios/{scenarioId}/versions/{version}` | 读取指定场景的输入、Skill 与成果定义 |
| `POST /api/v1/scenarios/{scenarioId}/versions` | 保存场景草稿与资源引用 |
| `POST /api/v1/scenarios/{scenarioId}/versions/{version}/publish` | 完成复核后发布场景版本 |
| `GET/POST /api/v1/algorithms` | 查询或登记算法能力及 OGE 绑定 |
| `GET/POST /api/v1/projects/{projectId}/memories` | 查询或保存项目记忆 |
| `PATCH/DELETE /api/v1/projects/{projectId}/memories/{memoryId}` | 更新、停用或删除记忆条目 |
| `GET/POST /api/v1/projects/{projectId}/events` | 查询或创建灾害事件，关联时间线与任务 |
| `GET /api/v1/tasks/{taskId}/timeline` | 获取观测帧、降雨过程和成果时间引用 |
| `PATCH /api/v1/tasks/{taskId}/reviews/{reviewId}` | 更新核查安排、现场反馈和复核状态 |
| `POST /api/v1/tasks/{taskId}/reviews/{reviewId}/attachments` | 上传核查照片等附件并保存时间和位置 |
| `GET /api/v1/assets` | 查询影像与辅助数据目录 |
| `POST /api/v1/assets` | 登记或上传业务数据资产 |
| `GET /api/v1/workflows` | 查询分析模板及参数定义 |
| `POST /api/v1/tasks` | 接收表单或 Agent 草案，校验后创建任务 |
| `GET /api/v1/tasks/{taskId}` | 查询配置、版本及执行状态 |
| `GET /api/v1/tasks/{taskId}/events` | 以 SSE 推送执行事件，支持断连后续接 |
| `GET /api/v1/tasks/{taskId}/results` | 获取当前成果清单与统计 |
| `GET /api/v1/results/{resultId}/layers` | 获取成果图层及空间参考 |
| `GET /api/v1/tasks/{taskId}/reviews` | 查询核查清单、意见和处理状态 |
| `GET /api/v1/tasks/{taskId}/report` | 获取当前版本报告 |
| `POST /api/v1/tasks/{taskId}/reviews` | 保存图斑核查标记 |
| `POST /api/v1/tasks/{taskId}/stop` | 停止后续编排并返回已提交任务情况 |
| `POST /api/v1/assistant/sessions` | 创建智能辅助会话并关联任务上下文 |
| `POST /api/v1/assistant/sessions/{sessionId}/messages` | 提交自然语言请求，生成草案或成果解读 |

前端采用 React 与 TypeScript 构建地图主工作台，OpenLayers 承担二维地图与专题图层，ECharts 承担动态图表，CesiumJS 承担重点片区三维地形查看。项目、事件、选区、活动时相、图层与成果版本组成共享页面状态；Agent 消息及工具结果引用同一组对象。计划参数以对话内卡片呈现，任务和成果通过抽屉查看，地图选择与查询过程持续保留。

地图采用结果预览与地理图层两种展示方式。OGE 的 COG 预览接口返回图片流，用于结果卡片查看；专题叠加使用具有空间参考的 GeoTIFF 或 GeoJSON，按范围和投影定位。浏览器通过后端成果接口读取数据，较大栅格在联调中配置 COG 与 Range 访问，矢量按显示尺度简化以提升交互速度，面积统计保留原始几何。

后端采用 Python/FastAPI 建设模块化应用，按 API 路由、应用服务、领域模型与基础设施组织代码。Pydantic 定义参数及结果模型，FastAPI 提供 REST 接口和 OpenAPI 描述，Uvicorn 运行 API 服务；Celery Worker 独立执行持久任务，与 API 共用领域服务和数据库。模型适配、运行内核、Tool、Skill、Memory 由团队自主实现，OGE 适配器集中处理鉴权、参数映射与结果引用。[FastAPI 功能文档](https://fastapi.tiangolo.com/features/)

完整案例的服务调用约定如下：`POST /api/v1/tasks` 接收 `projectId`、`eventId`、`scenarioId`、`scenarioVersion`、`aoi`、观测窗口、资产引用和参数，受理后返回 HTTP 202 及 `taskId`、状态和成果查询地址。客户端使用业务请求编号防止重复创建。任务完成后，成果接口返回图层、统计、核查清单与报告引用；API 文档列明单位、空间参考、鉴权范围及异常状态。

React、Agent 和外部客户端使用相同场景定义与应用服务。Agent 的 Tool 直接调用内部服务函数，外部系统通过 REST API 访问这些服务；OGE 数据与计算请求统一经过适配器。部署时提供 OpenAPI 文档、完整案例请求示例和结果字段说明，便于对接现有业务门户或 GIS 应用。

三维查看使用有授权的地形服务，或将示范区 DEM 制作为 CesiumJS 可读取的地形瓦片。二维与三维视图共享选区、成果和定位信息，按当前视野加载地形与图层；时间播放预取相邻观测帧，保留每帧实际日期与有效范围。[CesiumJS 地形文档](https://cesium.com/learn/cesiumjs-learn/cesiumjs-terrain/)

#### 4.8.2 运行环境表

系统开发与运行环境配置如下。

| 项目 | 拟采用配置 |
| --- | --- |
| 体系结构 | B/S、前后端分离；React—FastAPI 服务—OGE 平台协作 |
| 开发平台 | OGE 开发中心与开放服务，本地 Windows/macOS/Linux 环境 |
| 开发工具 | VS Code、Git、npm、uv、OGE 在线编辑器；QGIS 核验成果，ESA SNAP 准备 SAR 数据 |
| 开发语言 | 前端 TypeScript/TSX、HTML/CSS；后端、自研 Agent 和 OGE 脚本使用 Python |
| 前端框架 | React、OpenLayers、ECharts；重点片区三维展示使用 CesiumJS |
| 后端框架 | FastAPI、Pydantic、Uvicorn；HTTPX 适配 OGE 与模型接口 |
| 后台执行 | Celery Worker 与 Redis 队列，任务图及恢复状态保存到 PostgreSQL |
| 数据库 | PostgreSQL/PostGIS；SQLAlchemy 管理数据访问，Alembic 管理结构变更 |
| Agent 实现 | Python 自研插件容器、能力接口、事件日志、Tool、Skill、Memory、策略门禁与模型适配；参考 DeepSeek Harness 架构思想 |
| 场景资源 | 版本化 SKILL.md、算法绑定、工作流、质量规则、表单与成果模板 |
| 记忆存储 | PostgreSQL 结构化字段与中文分词关键词索引；按项目、场景、条件和版本检索 |
| 数据格式 | GeoTIFF/COG、GeoJSON、CSV、JSON、PNG、Markdown；三维地形使用相应瓦片格式 |
| 部署方式 | Docker Compose 组织 Nginx、API、Worker、调度进程、PostgreSQL/PostGIS 与 Redis；持久化挂载数据库和成果目录 |
| 运行环境 | Linux；Nginx 提供 React 静态资源、HTTPS 与反向代理；Uvicorn 承载 FastAPI；OGE 托管遥感计算 |
| 资源预算 | 示范部署暂按 4 vCPU、8 GB 内存、100 GB 磁盘规划，按任务、地形与成果规模实测调整；OGE 计算资源另计 |
| 网络与凭证 | OGE 与模型服务网络，授权数据访问；账号、AppKey 和模型凭证由后端管理 |

前端依赖通过 npm 管理，Python 后端与本地脚本通过 uv 管理环境和锁定依赖，OGE 脚本使用平台提供的运行环境。部署包包含服务配置、数据库迁移、健康检查、持久目录和操作说明。示范部署配置低并发 Worker，记录 API 响应、作业排队、OGE 计算和报告生成耗时，再据实调整资源。

#### 4.8.3 自主代码结构与模块协作

代码按前端、后端和 OGE 脚本组织，主要目录设计如下。

```text
web/
  src/features/       地图工作台、Agent、事件、核查与方法管理
  src/components/     图层、时间轴、图表、计划卡片与三维视图
  src/api/            REST 接口与 SSE 事件订阅
server/
  pyproject.toml      Python 项目与依赖定义
  uv.lock             依赖锁定
  app/main.py         FastAPI 启动与组件装配
  app/api/            路由、请求模型、鉴权与事件流
  app/services/       项目、事件、数据、场景、任务、核查和报告
  app/domain/         业务对象、工作流规则与状态转换
  app/agent/
    runner/           运行状态机与事件处理
    model/            模型适配与流式响应
    tools/            Tool 定义、注册与执行
    skills/           Skill 发现、加载及依赖解析
    memory/           分层记忆、检索、更新与失效管理
    context/          上下文组装、摘要与预算
    session/          会话、运行与工具轨迹
  app/integrations/oge/ 平台接口、凭证、算法映射与成果引用
  app/workers/        Celery 任务、步骤领取与结果登记
  app/scheduler/      持久事件投递、状态查询安排与恢复
  app/storage/        数据库访问与成果文件管理
  migrations/         数据库结构变更
scenarios/            Skill、流程、质量规则及成果配置
oge-scripts/
  flood/              水体识别、变化提取与影响统计
  recovery/           灾后植被指数、可比时序与分区统计
deploy/               容器编排、反向代理和环境配置说明
```

各模块通过明确的服务接口协作：任务服务调用 OGE 适配器完成计算，成果服务登记输出，核查和报告服务读取成果；Agent 工具调用应用服务，React 与外部系统通过 FastAPI 使用相同业务能力。业务数据结构、Agent 协议与工具定义由团队设计，并随接口文档和部署说明交付。

### 4.9 验证方法与验收指标

#### 4.9.1 遥感结果验证

验证分为输入核验、分类精度核验和统计一致性核验。首先检查数据日期、波段、投影、有效范围和处理级别；其次在研究区分层抽取新增积水、稳定水体、非水体和易混淆区域样本；最后核查矢量叠加、面积单位及乡镇汇总是否一致。

验证以空间独立的图斑或样区为抽样单元，按新增水体、稳定水体、非水体及河岸、耕地、建筑周边、阴影和水田等易混淆类型分层抽样，样本量由类别面积、预期误差和可获取真值共同确定，不以“不少于 200 个”代替抽样设计。阈值校准区与验证区采用空间分块隔离，避免相邻像元泄漏。每个样本记录采样方式、判读时间与参考资料；关键样本由两名判读者独立标注并报告一致性，有争议的样本进入复核集合。单一榕江事件只用于案例验证，不外推跨区域泛化性。

验证参考信息优先选用接近目标观测时间的高分辨率影像、现场记录和独立解译成果，按空间位置与日期建立对应关系。采用输入影像辅助人工判读的样本单独记录参考类型，评价结果同时说明样本来源、时间匹配程度和判读一致性。

分类评价重点报告新增积水类别的 Precision、Recall、F1 和 IoU，结合混淆矩阵和样本数量分析误检与漏检。按地物类型整理代表性样例，定位河岸、湿土、水田和阴影区域的主要误差。涉及区域面积推断时，根据分层抽样设计进行面积加权，使评价结果与地表类别分布相匹配。

面积核验使用相同坐标和有效范围，抽查乡镇级汇总与图斑合计、栅格与矢量计算是否一致。参数敏感性验证围绕校准阈值和最小图斑面积做小范围变化，记录面积变化及主要图斑稳定性，据此解释方法的适用边界。

#### 4.9.2 工程与业务验收

项目从脚本运行、遥感质量、交互效率和业务使用四个方面设置验收目标，形成可记录、可复查的评价结果。

| 验收项 | 目标与测量方式 |
| --- | --- |
| 必选脚本 | 至少一套 OGE 主脚本可独立运行，产出水体变化、影响统计和地图 |
| 数据可追溯 | 所有正式成果具备输入标识、日期、参数、步骤和结果引用，统计字段与计算结果逐项关联 |
| 分类质量 | 在共同有效观测、可可靠判读的开放水面范围内，以简单 MNDWI/SAR 阈值为基线，报告 Precision、Recall、F1、IoU 及 95% 置信区间；F1=0.80 仅作为预设工程目标，未达到时如实报告并分析误差 |
| 结果完整性 | 无效观测区、零检测结果、数据缺失和任务失败四种情况明确区分 |
| 交互响应 | 在约定部署环境中，任务受理接口响应 P95 初设目标不超过 3 秒，计算耗时按任务步骤单独记录 |
| 工作效率 | 使用同一任务、数据和成功定义，对比人工多工具、固定表单和 Agent 场景流程的操作步数、主动操作耗时、错误率和端到端耗时，不预先承诺减幅 |
| 统计一致性 | 地图、统计表及报告使用同一结果版本；面积差异可解释并可追查 |
| 产品可用性 | 用户从项目进入场景，完成任务提交、地图查看、图斑复核与报告导出，并能重新打开历史成果 |
| Tool 执行 | 工具输入输出可校验，异步任务可追踪，工具结果与页面业务对象一致 |
| Skill 使用 | 能够按场景发现、加载、验证和发布版本，任务固定所用版本及算法绑定 |
| Memory 连续性 | 会话恢复后保留任务关联；新任务可读取项目设置，按范围检索、更新和停用经验条目 |
| 场景扩展 | 发布并运行灾后植被状态跟踪示例，展示算法、Skill、时序与报告的组合 |
| 多源应用 | 逐项记录光学、SAR、DEM、降雨及承灾对象的用途，核对其参与处理或研判的结果 |
| 时序交互 | 逐帧日期和有效覆盖可查询，地图、动态图表与时间轴同步 |
| 三维示范 | 完成重点片区地形与积水、设施叠加，检查二维三维定位和来源信息 |
| 服务集成 | 使用独立 REST 客户端提交同一案例，取得与工作台一致的任务及成果版本 |
| 核查闭环 | 完成对象安排、现场反馈、复核与报告更新，历史版本可回查 |
| 异常处理 | 服务未发布、凭证过期、数据缺失、任务失败、提交状态不明均有明确状态和处理入口 |
| 重复性 | 相同数据、参数和算法版本下可复算，并解释平台运行环境引起的数值差异 |

精度评价以共同有效观测内的开放水面为主要对象，复杂建筑区和林下淹水另作专项分析。验收材料包含完整样本说明、分类误差、指标结果和方法改进记录。运行效率分别记录 OGE 排队、计算、网络及模型调用耗时，工程验收保存联调记录、操作截图和问题修订说明。

#### 4.9.3 Agent 与场景扩展验证

建立不少于 30 条的固定任务集，覆盖正常请求、地名或时间歧义、无有效覆盖、波段不匹配、权限失败、工具超时、多源冲突和任务恢复。分别运行“固定表单/工作流”“LLM + Tool”“LLM + Tool + Skill”和完整系统，比较意图槽位准确率、合法计划率、工具与参数正确率、任务成功率、无依据结论率、异常恢复率、人工干预次数、耗时和模型用量。

场景扩展以“灾后植被状态跟踪”为第二场景，记录从导入场景包到生成表单、计划、图层、统计和报告的全过程。核心验收条件是 Agent 运行底座无改动，通用 API 无业务特判；如新增了数据或展示适配插件，则单独列出改动，不将其表述为“零代码接入”。

### 4.10 主要难点及实施措施

| 难点 | 对作品的影响 | 具体措施 |
| --- | --- | --- |
| 汛期光学影像受云雨影响 | 事件期覆盖不完整 | 提前核实 SAR 输入；按有效范围分析，对缺测区列补充观测清单 |
| 灾前基准不稳定或观测晚于洪峰 | 低估变化或误读事件范围 | 关联实际观测日期与事件时间线，说明积水发展或消退阶段 |
| 地形阴影、水田灌溉及水岸错位 | 产生伪新增水体 | 质量掩膜、输入对齐、独立样本核验和图斑复核 |
| 算子尚未发布或权限不足 | Web 调用链无法完成 | 第 1 周核实权限并申请发布；脚本能力和接口发布状态分别管理 |
| 多模块协作与平台接入 | 业务字段和外部协议需要统一 | 明确 Python 服务接口、Agent 工具定义及 OGE 协议，按模块开展联调 |
| 大栅格和长时间任务 | 页面卡顿、计算重复 | OGE 内部衔接中间结果；后端管理异步依赖；地图分级展示 |
| 图表与报告同步 | 结果更新后各项成果需要保持一致 | 结构化结果驱动报告，统一版本与字段引用，导出前复核 |
| 开发周期有限 | 遥感与前端同时铺开导致主链延期 | 优先完成脚本与示范案例，再开展服务、编排和展示 |

### 4.11 场景扩展与方法沉淀机制

#### 4.11.1 场景包与通用能力的关系

工作台将区域、资产、任务、图层、表格、核查和报告作为通用对象，将每个专业应用组织为一个场景包。场景包由 OGE 算法绑定、Skill、步骤模板、质量规则和成果配置组成，发布后出现在场景库中，供项目选择使用。

洪涝场景调用水体提取与变化分析，灾后植被状态跟踪场景调用植被指数计算与时序统计，两者都可以复用区域选择、数据检索、任务调度、栅格显示、分区汇总、成果保存和报告导出。Tool 使用通用业务接口，Agent 通过所选 Skill 了解本次分析的数据要求、步骤与解释方法。

扩展由三个层次协作完成：遥感人员提供可用的算法与评价方法，场景维护者将处理经验组织为 Skill 和配置，工作台负责加载、执行及展示。随着案例积累，同一场景可以拥有更成熟的数据选择规则和参数建议。

#### 4.11.2 新增场景的实施步骤

1. **定义业务问题与成果。** 明确研究对象、空间范围、时间尺度和用户需要的图层、指标及报告内容。
2. **准备 OGE 计算能力。** 选择平台算子，编写或调整脚本及组合模型，确定输入数据、单位、预处理条件和输出类型，完成小范围结果验证。
3. **登记算法绑定。** 在算法目录中登记逻辑能力、OGE 服务和参数映射，检查数据类型与任务执行方式。
4. **编写并沉淀 Skill。** 将数据选择、步骤组合、条件分支、质量检查和成果解释写入 Skill；补充工作流模板、参考依据和验证案例。
5. **配置页面与成果。** 定义参数表单、栅格色带、矢量样式、统计字段和报告模板，关联到场景清单。
6. **验证并发布。** 在同一工作台完成一次场景任务，检查计算、展示、导出和追踪记录，发布场景版本并加入场景库。
7. **积累案例与改进方法。** 将完成任务保存为案例，整理复核经验，按版本更新 Skill 和算法配置。

常见栅格、矢量和统计表成果使用通用渲染组件；出现新的结果类型或业务操作时，在对应 Python 服务和 React 组件中增加适配。例如，增加状态跟踪指数图可配置栅格色带与统计字段，增加新的灾害专用结果类型时扩展相应展示组件。场景管理记录这些依赖，使开发工作能够按新增能力定位到具体模块。

#### 4.11.3 灾害监测与研判场景扩展

场景建设从洪涝影响分析延伸至灾后植被状态跟踪。本期在榕江县完成积水识别、耕地疑似暴露统计与重点对象核查主链，并选择少量受关注地块配置灾后植被状态跟踪场景；农业干旱、火烧迹地和地质灾害迹地核查只作为后续扩展设计。

| 场景与建设安排 | OGE 算法建设 | Skill 沉淀重点 | 决策支撑成果 |
| --- | --- | --- | --- |
| 洪涝影响分析：本期完整示范 | 光学/SAR 水体提取、变化与承灾对象叠加 | 基准选择、多源质量、影响统计及核查规则 | 变化图、乡镇统计、重点核查清单和报告 |
| 灾后植被状态跟踪：本期轻量扩展示范 | 植被指数、可比时相筛选、分区与时序统计 | 历史同期或未受灾对照、物候与地类条件、持续异常片区核查 | 地块时序曲线、相对参考变化与持续观察清单 |
| 农业干旱影响：后续场景 | 降雨距平、植被异常与地表温度专题组合 | 历史同期基准、作物历、多指标解释与核验 | 异常片区、农业核查对象及阶段对比 |
| 火烧迹地与恢复：后续场景 | 火烧迹地指数、灾前灾后变化与覆盖统计 | 事件窗口、地物混淆、样本校准及恢复观察 | 火烧迹地候选、覆盖影响和恢复时间序列 |
| 地质灾害迹地核查：后续场景 | 地表变化、DEM 地形及道路居民点叠加 | 事件前后判读、地形条件和现场复核 | 变化候选、关联对象与专业核查线索 |

灾后植被状态跟踪示范选取洪涝案例中具有连续有效观测的受关注地块，增加灾后多期光学影像。OGE 计算植被指数、质量掩膜和分区统计，以可比的历史同期观测或未受灾地块为对照，记录地类、种植周期、管理活动和有效覆盖。界面展示指数变化及其与参考状态的差异，结合现场记录筛选需要继续观察的地块，不将单一指数变化直接归因于洪涝。

状态跟踪场景在算法目录登记植被指数与时间序列统计服务，通过 Skill 规定波段缩放、云影筛选、时相选择、参考建立和判读方法，并配置指数图层、变化曲线与观察清单。场景发布后，Agent 调用现有数据检索、工作流提交和成果查询工具执行分析，地图按对应样式加载结果，时间轴同步呈现后续观测。

#### 4.11.4 算法、Skill 与 Memory 的协同演进

算法版本记录计算方法与参数接口，Skill 版本记录业务组织与解释方法，Memory 保存具体项目的设置、案例和经复核经验。一次任务将三者的使用关系写入运行记录，形成可回查的方法依据。

算法调整时，检查参数映射、输出字段和依赖该算法的 Skill；Skill 调整时，选择代表性案例验证步骤与解释；案例复核产生新认识时，先形成经验条目，再由技术人员整理进方法版本。通过这一过程，项目中的经验能够成为其他项目可用的场景能力。

### 4.12 作品特色、评审对应与应用价值

| 评审维度 | 方案回应 | 重点交付证据 |
| --- | --- | --- |
| 业务场景价值与逻辑闭环 | 以 2025 年榕江洪涝为真实案例，将“数据接入—信息提取—影响研判—核查反馈—成果更新”连成业务链 | 乡镇统计、疑似暴露对象、核查清单、反馈记录和前后成果版本 |
| OGE 资源编排与深度应用 | 在 OGE 内完成数据检索、光学水体变化、中间成果引用、承灾对象叠加和分区统计；在条件允许时增加 SAR、DEM 和降雨辅助观测 | 可独立运行的 OGE 脚本、算子/组合模型调用、`processId` 与前序成果引用链 |
| 系统架构与工程落地 | React—FastAPI—Worker—OGE 前后端分离，同一场景能力由表单、Agent 和 REST 客户端复用 | WebService 请求与结果示例、异步任务恢复、数据和方法版本锁定 |
| 交互体验与技术先进性 | 地图选区、观测时间轴、图斑、统计与 Agent 共享空间语境；GeoSkill 约束 LLM 产生可执行计划 | “这里/上一期/这个图斑”联动演示、失败请求的受控拒绝、第二场景接入演示 |
| 方案完整性与文案规范 | 严格区分观测、疑似暴露、影响和灾损，明确尺度、有效范围、不确定性和实施边界 | 输入数据清单、方法与参数、精度评价、误差样例、部署和操作说明 |

作品最具辨识度的技术主张是“**一套底座、一个场景契约、一条可追溯证据链**”。GeoSkill 场景包同时驱动 Agent 计划、确定性工作流、质量校验、页面展示和报告字段；OGE 提供遥感计算与中间成果衔接；Agent 只在契约和授权内组织任务。这使方法可复用、任务可恢复、结论可反查，也使表单、Agent 和外部系统能复用同一条业务能力。

业务上，平台不将遥感结果直接宣称为实际灾损，而是转化为乡镇、耕地、居民点和道路层面的核查线索，保留观测时刻、有效范围和证据状态。人工反馈先进入项目案例，只有经技术复核并说明适用条件的经验才能进入新的 GeoSkill 版本，避免个别误判自动污染后续分析。

## 五、作品制作周期

### 5.1 总体安排

项目拟按 **8 周**组织开发，具体起止日期结合赛程安排确定。开发依次完成数据准备、核心脚本、服务编排、应用集成和案例验收。第 1 周落实平台账号、服务发布权限及数据清单，第 2 周完成影像与辅助资料准备。

| 阶段 | 时间 | 主要工作 | 阶段交付与检查点 |
| --- | --- | --- | --- |
| 产品与资源设计 | 第 1 周 | 确定工作台页面、业务对象和 Tool/Skill/Memory 接口；核实贵州案例数据及 OGE 资源 | 产品流程、接口设计、数据与算法能力清单 |
| 数据与场景准备 | 第 2 周 | 准备光学、SAR 及辅助数据，组织样本；定义洪涝 Skill、场景清单和成果配置 | 数据包、校准方案、洪涝场景包草稿 |
| 核心计算与业务开发 | 第 3—4 周 | 完成 OGE 水体变化与影响统计；开发 Python 数据、任务与成果服务，搭建 React 地图工作台与计划卡片 | 独立脚本、首套案例成果，以及可提交并查看任务的业务流程 |
| Agent 与能力管理 | 第 5 周 | 集成运行循环、Tool 注册、Skill 加载、Memory 检索写入和任务事件 | 在项目内完成自然语言分析与会话恢复，保存方法及记忆关联 |
| 工作台与扩展场景 | 第 6 周 | 完成地图统计、时间轴播放、核查回填、报告和场景管理；配置灾后植被状态跟踪示例 | 完整洪涝业务流程、轻量扩展场景包与项目记忆管理 |
| 集成与扩展验证 | 第 7 周 | 检查洪涝精度与业务流程，验证 Tool、Skill 版本、Memory 范围及跨场景复用；主链稳定后再实现三维加分项 | 质量报告、Agent 对照验证、时序与扩展演示、独立 API 调用及修订记录 |
| 材料与演示 | 第 8 周 | 整理方案、部署说明、用户手册、案例包和演示视频 | 可交付版本、材料清单、现场演示流程及归档成果 |

### 5.2 关键里程碑与依赖

第 2 周完成案例影像对、辅助资料和研究范围确定，形成可进入算法开发的数据包；第 4 周完成 OGE 主脚本及第一套完整分析成果；第 5 周打通 Tool、Skill、Memory 与任务执行；第 6 周形成完整工作台和灾后植被状态跟踪示例；第 7 周完成结果质量、Agent 有效性、业务流程及扩展验收；第 8 周形成提交版本与演示材料。三维、SAR 增强分支等加分项只在不影响核心链路的前提下纳入当期里程碑。

前端基础页面与脚本开发可同步推进，统计和专题图模块在第一套成果生成后完成数据接入。演示围绕“进入项目—选择场景—运行分析—查看成果—复核导出—复用设置—扩展场景”展开，同时提供已完成案例的历史记录，便于展示不同任务的分析过程与结果。

### 5.3 开发完成后的交付清单

| 交付物 | 内容与完整性要求 |
| --- | --- |
| 应用开发方案 | 作品概述、需求、功能、开发方案、制作周期与团队分工，含架构图和流程图 |
| OGE 脚本包 | 主脚本、业务子脚本、参数说明、运行入口和示例任务 |
| 数据目录 | 数据来源、时间、许可、投影、预处理、资产引用及获取方式 |
| 服务说明 | 已发布服务清单、参数、输出、鉴权要求和调用示例 |
| React Web 平台 | 地图主工作台、Agent 面板、数据图层、动态图表、观测时间轴、核查报告、任务成果抽屉与方法管理 |
| 三维地形示范（加分项） | 主链稳定后，完成重点片区地形数据、积水与设施叠加、二维三维联动及来源说明 |
| Python/FastAPI 业务后端 | 项目、场景、算法目录、数据、任务、成果、核查和报告服务，OGE 适配及持久化存储 |
| 自研 Agent 模块 | 架构与源码，Tool 定义、Skill 加载、Memory 检索更新、运行内核、模型适配与执行记录 |
| 场景与方法包 | 完整洪涝 Skill、算法绑定、流程、质量和成果配置；灾后植被状态跟踪轻量扩展示例 |
| 记忆与案例说明 | 会话恢复、项目设置、案例摘要、经验复核与失效管理的操作说明 |
| 示范案例成果 | 案例影像对、变化图层、有效范围、耕地统计、核查清单和报告 |
| 验证材料 | 样本说明、精度指标、统计核验、耗时记录及典型误差 |
| 操作与部署手册 | 环境、凭证配置、任务提交、结果导出及常见错误处理 |
| 演示材料 | 演示视频、任务操作流程和历史案例展示 |

## 六、团队介绍（包括个人专业介绍及分工）

### 6.1 团队组成与协作方式

团队围绕遥感方法、平台脚本、Python/FastAPI 业务后端、自研 Agent 和 React WebGIS 展示设置职责。实际成员可以兼任多个方向，但每个关键交付物指定一名负责人，保证数据口径、算法结果和应用展示之间有人协调。

团队实行每周一次的进度与成果检查，以脚本、计算成果、接口联调和页面操作作为阶段检查内容。跨模块事项明确负责人、输入输出和完成时间。提交前由遥感负责人复核结果口径，工程负责人复核部署与运行记录，队长统筹方案、应用和演示材料的一致性。

各岗位分工及交付责任如下。

| 成员或岗位 | 姓名与专业背景 | 主要分工 | 负责交付 |
| --- | --- | --- | --- |
| 队长／总体协调 | 【姓名、专业、年级或岗位、项目经历】 | 需求、总体架构、进度与最终材料协调；统一统计与成果口径 | 方案、总体集成、提交材料 |
| 遥感与 OGE 脚本负责人 | 【姓名、专业、遥感与 GIS 相关经历】 | 数据准备、SAR 预处理、OGE 脚本、校准与精度核验；场景 Skill 和经验整理 | 脚本包、数据目录、验证材料 |
| Python/FastAPI 后端与 Agent 负责人 | 【姓名、专业、Python 后端与智能体开发经历】 | 业务服务、任务调度、存储与 OGE 接入；自主设计 Agent 运行内核、Tool、Memory 与 Skill 管理 | Python 业务后端、Agent 源码与架构说明、接口文档 |
| React 前端与 WebGIS 负责人 | 【姓名、专业、React 与 WebGIS 开发经历】 | 地图主界面、时间轴与动态图表、三维地形、核查交互、Agent 面板和成果导出 | React Web 应用、操作手册、演示材料 |
| 指导老师（如有） | 【姓名、单位、职称及实际指导方向】 | 方法与应用场景指导，复核关键技术和成果表述 | 指导意见与成果审阅 |

岗位根据成员专长安排，相关职责可由同一成员兼任。数据与算法模块向后端提供标准化输入和成果字段，后端向前端提供任务与结果接口，前端反馈交互及业务使用问题，各负责人共同完成集成验证。

### 6.2 开发组织与成果管理

项目依据 OGE 开发文档与 OpenAPI 手册设计遥感计算和平台接入，采用 React、Python/FastAPI 自主建设项目工作台、场景管理与分析服务。Agent 参考 DeepSeek Harness 的插件化、能力接缝和事件日志思路，由团队自研 Python GeoAgent Harness、OGE 异步任务适配、空间语境引用、遥感质量门禁和 GeoSkill 场景契约。方案、代码、服务配置与操作说明统一纳入版本管理，阶段成果按任务和日期归档；答辩时附自研边界、第三方依赖和许可证清单。

团队协作重点落在三个环节：数据与算法负责人共同确定影像质量和评价样本；算法与后端负责人共同核对算子参数、结果类型及任务依赖；后端与前端负责人共同验证地图、统计和报告的一致性。通过明确的模块接口和阶段成果检查，保证各部分工作按统一口径推进。

---

## 参考资料

网络资料查阅日期：2026 年 9 月 9 日。

1. 《东方航天港杯遥感应用案例开发方案模版》。
2. [OGE 应用开发帮助文档](https://www.openge.org.cn/docs/oge-application-develop)，快速开始、API、算子介绍及应用开发案例。
3. 《OGE OpenAPI 开发者手册》，接口协议、鉴权、异步执行与结果引用。
4. Copernicus Data Space Ecosystem：[Sentinel-2 L2A 技术文档](https://documentation.dataspace.copernicus.eu/APIs/SentinelHub/Data/S2L2A.html)、[Sentinel-1 GRD 技术文档](https://documentation.dataspace.copernicus.eu/APIs/SentinelHub/Data/S1GRD.html)。
5. ESA STEP：[Sentinel-1 Flood Mapping Tutorial](https://step.esa.int/docs/tutorials/tutorial_s1floodmapping.pdf)。
6. McFeeters, S. K. (1996). The use of the Normalized Difference Water Index (NDWI) in the delineation of open water features. *International Journal of Remote Sensing*, 17(7), 1425–1432. [DOI](https://doi.org/10.1080/01431169608948714)。
7. Xu, H. (2006). Modification of normalised difference water index (NDWI) to enhance open water features in remotely sensed imagery. *International Journal of Remote Sensing*, 27(14), 3025–3033. [DOI](https://doi.org/10.1080/01431160600589179)。
8. DeepSeek Harness，README 与 Architecture，“everything is a plugin”、能力接缝、类型事件与会话日志的设计参考：[DeepSeek Harness](https://github.com/deepseek-ai/deepseek-harness)；本地阅读版本 `5dda764ed3`。

9. FastAPI：[框架功能](https://fastapi.tiangolo.com/features/)、[后台任务](https://fastapi.tiangolo.com/tutorial/background-tasks/)、[容器部署](https://fastapi.tiangolo.com/deployment/docker/)。
10. Celery：[任务文档](https://docs.celeryq.dev/en/stable/userguide/tasks.html)。
11. CesiumJS：[三维地形展示](https://cesium.com/learn/cesiumjs-learn/cesiumjs-terrain/)。

12. 应急管理部：[《国家防灾减灾救灾委员会办公室 应急管理部发布2025年全国自然灾害情况》](https://www.mem.gov.cn/xw/yjglbgzdt/202601/t20260116_592135.shtml)，2026-01-16。
13. 应急管理部：[《国家防总针对重庆、贵州启动防汛四级应急响应 派出工作组赴贵州指导防汛抢险》](https://www.mem.gov.cn/xw/yjglbgzdt/202506/t20250624_547360.shtml)，2025-06-24。
14. 贵州省水利厅：[《榕江、从江争分夺秒，加快保障灾后城乡供水》](https://mwr.guizhou.gov.cn/gzdt/slyw/202506/t20250627_88196074.html)，2025-06-27。
15. 贵州省国资委：[《清淤排险助榕江 贵州建投在行动》](https://gzw.guizhou.gov.cn/xwzx/qyzc/202506/t20250630_88212673.html)，2025-06-30。
16. 贵州省交通运输厅：[《截至6月24日10时国省干线公路阻断信息》](https://jt.guizhou.gov.cn/cxfw_0/lkxx_5948603/gxgllk_5948605/zdxx_5948607/202506/t20250624_88180838.html)，2025-06-24。
